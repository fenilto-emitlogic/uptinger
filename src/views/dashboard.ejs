<!DOCTYPE html>
<html lang="en"
    class="h-full text-[var(--color-text-primary)] selection:bg-[color-mix(in_srgb,var(--color-primary)_30%,transparent)] selection:text-[var(--color-primary)] bg-[var(--color-bg-main)]">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link rel="icon" type="image/png" href="/favicon/favicon-96x96.png" sizes="96x96">
    <link rel="icon" type="image/svg+xml" href="/favicon/favicon.svg">
    <link rel="shortcut icon" href="/favicon/favicon.ico">
    <link rel="apple-touch-icon" sizes="180x180" href="/favicon/apple-touch-icon.png">
    <link rel="manifest" href="/favicon/site.webmanifest">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=JetBrains+Mono:wght@400;500&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <script src="/js/toast.js"></script>
    <script src="/js/modal.js"></script>

</head>

<body class="min-h-screen flex flex-col text-[var(--color-text-primary)] bg-[var(--color-bg-main)]">

    <%- include('partials/header', { user, orgs: (typeof orgs !=='undefined' ? orgs : []), currentOrg: (typeof
        currentOrg !=='undefined' ? currentOrg : null), page: 'dashboard' }) %>

        <!-- Main Dashboard Grid -->
        <main class="flex-1 max-w-[1700px] w-full mx-auto px-4 sm:px-6 lg:px-8 py-6">

            <% if (sites.length===0) { %>
                <!-- FULL SCREEN BEAUTIFUL EMPTY STATE -->
                <div
                    class="py-16 flex flex-col items-center justify-center text-center space-y-6 bg-[color-mix(in_srgb,var(--color-bg-surface)_40%,transparent)] border border-[color-mix(in_srgb,var(--color-border)_70%,transparent)] rounded-3xl p-12 shadow-2xl max-w-3xl mx-auto my-8">
                    <div class="relative">
                        <div
                            class="w-24 h-24 rounded-3xl bg-gradient-to-tr from-[color-mix(in_srgb,var(--color-primary)_20%,transparent)] via-[color-mix(in_srgb,var(--color-secondary)_10%,transparent)] to-[color-mix(in_srgb,var(--color-secondary)_20%,transparent)] border border-[color-mix(in_srgb,var(--color-primary)_30%,transparent)] flex items-center justify-center shadow-[0_0_40px_color-mix(in_srgb,var(--color-primary)_15%,transparent)]">
                            <svg class="w-12 h-12 text-[var(--color-primary)]" fill="none" stroke="currentColor"
                                viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5"
                                    d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                            </svg>
                        </div>
                        <span
                            class="absolute -bottom-1 -right-1 w-6 h-6 rounded-full bg-[var(--color-primary)] border-4 border-[var(--color-bg-main)] animate-pulse"></span>
                    </div>

                    <div class="max-w-md space-y-2">
                        <h2 class="text-2xl font-black text-center text-[var(--color-text-primary)] tracking-tight">No
                            Monitors
                            Configured <span class="text-[var(--color-primary)]">Yet</span></h2>
                        <p class="text-sm text-[var(--color-text-secondary)] leading-relaxed">
                            Welcome to Uptinger! Get started by adding your first endpoint or service to monitor its
                            uptime,
                            SSL certificates, and response times in real time.
                        </p>
                    </div>

                    <div class="pt-2">
                        <a href="/monitors/new"
                            class="px-8 py-3.5 rounded-2xl bg-[var(--color-primary)] hover:bg-[var(--color-primary)] text-[var(--color-bg-main)] font-black text-sm tracking-wide shadow-xl shadow-[color-mix(in_srgb,var(--color-primary)_25%,transparent)] transition-all duration-200 flex items-center space-x-2.5 active:scale-95">
                            <svg class="w-5 h-5 stroke-[2.5]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" d="M12 4v16m8-8H4" />
                            </svg>
                            <span>Add Your First Monitor</span>
                        </a>
                    </div>
                </div>
                <% } else { %>
                    <!-- DASHBOARD GRID WHEN MONITORS EXIST -->
                    <div class="grid grid-cols-1 lg:grid-cols-12 gap-6">

                        <!-- LEFT SIDEBAR COLUMN: Monitors List Card -->
                        <aside class="lg:col-span-4 xl:col-span-3 space-y-4">
                            <div
                                class="bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)] border border-[color-mix(in_srgb,var(--color-border)_80%,transparent)] rounded-2xl p-4 shadow-xl space-y-4">

                                <!-- Filter Controls -->
                                <div class="space-y-2">
                                    <div class="grid grid-cols-3 gap-2">
                                        <div class="relative">
                                            <select id="statusFilter" onchange="filterMonitors()"
                                                class="w-full appearance-none bg-[color-mix(in_srgb,var(--color-bg-main)_80%,transparent)] border border-[var(--color-border)] rounded-xl px-3 py-1.5 pr-7 text-xs font-medium text-[var(--color-text-secondary)] focus:outline-none focus:border-[color-mix(in_srgb,var(--color-primary)_50%,transparent)] cursor-pointer">
                                                <option value="ALL">Status ▾</option>
                                                <option value="ONLINE">Up</option>
                                                <option value="OFFLINE">Down</option>
                                                <option value="PAUSED">Paused</option>
                                            </select>
                                            <svg class="w-3.5 h-3.5 text-[var(--color-text-secondary)] absolute right-2.5 top-2.5 pointer-events-none"
                                                fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                                    d="M19 9l-7 7-7-7" />
                                            </svg>
                                        </div>
                                        <div class="relative">
                                            <select id="typeFilter" onchange="filterMonitors()"
                                                class="w-full appearance-none bg-[color-mix(in_srgb,var(--color-bg-main)_80%,transparent)] border border-[var(--color-border)] rounded-xl px-3 py-1.5 pr-7 text-xs font-medium text-[var(--color-text-secondary)] focus:outline-none focus:border-[color-mix(in_srgb,var(--color-primary)_50%,transparent)] cursor-pointer">
                                                <option value="ALL">Type ▾</option>
                                            </select>
                                            <svg class="w-3.5 h-3.5 text-[var(--color-text-secondary)] absolute right-2.5 top-2.5 pointer-events-none"
                                                fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                                    d="M19 9l-7 7-7-7" />
                                            </svg>
                                        </div>
                                        <div class="relative">
                                            <select id="tagFilter" onchange="filterMonitors()"
                                                class="w-full appearance-none bg-[color-mix(in_srgb,var(--color-bg-main)_80%,transparent)] border border-[var(--color-border)] rounded-xl px-3 py-1.5 pr-7 text-xs font-medium text-[var(--color-text-secondary)] focus:outline-none focus:border-[color-mix(in_srgb,var(--color-primary)_50%,transparent)] cursor-pointer">
                                                <option value="ALL">Tags ▾</option>
                                            </select>
                                            <svg class="w-3.5 h-3.5 text-[var(--color-text-secondary)] absolute right-2.5 top-2.5 pointer-events-none"
                                                fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                                    d="M19 9l-7 7-7-7" />
                                            </svg>
                                        </div>
                                    </div>
                                </div>

                                <!-- Monitors List -->
                                <div id="monitorsList" class="space-y-2 max-h-[calc(100vh-190px)] overflow-y-auto pr-1">
                                    <% sites.forEach((site, index)=> { %>
                                        <% const isPaused=site.is_paused; const isUp=!isPaused && site.status==='ONLINE'
                                            ; const chipLabel=isPaused ? 'Paused' : (isUp ? 'Up' : 'Down' ); const
                                            chipColorVar=isPaused ? '--color-warning' : (isUp ? '--color-primary'
                                            : '--color-error' ); %>
                                            <div onclick="selectMonitor(<%= site.id %>)"
                                                id="monitor-card-<%= site.id %>"
                                                class="monitor-item group cursor-pointer bg-[color-mix(in_srgb,var(--color-bg-main)_40%,transparent)] hover:bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)] border border-[color-mix(in_srgb,var(--color-border)_70%,transparent)] rounded-xl p-3 transition-all duration-200 relative overflow-hidden <%= index === 0 ? 'border-[var(--color-primary)] bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)] shadow-[inset_0_0_0_1px_var(--color-primary)]' : '' %>">

                                                <div class="flex items-center justify-between mb-2">
                                                    <span
                                                        class="font-semibold text-sm text-[var(--color-text-primary)] group-hover:text-[var(--color-primary)] transition-colors truncate">
                                                        <%= site.name %>
                                                    </span>
                                                </div>

                                                <!-- Dynamic Heartbeat Bar Visualization: fixed grid of cells, filled from the right, pending indicator at the right end -->
                                                <div class="flex items-center space-x-1 w-full overflow-hidden">
                                                    <% const hbCellCount=24; const hbList=(site.heartbeats ||
                                                        []).slice(-hbCellCount); const hbEmptyCount=Math.max(0,
                                                        hbCellCount - hbList.length); %>
                                                        <% for (let i=0; i < hbEmptyCount; i++) { %>
                                                            <span
                                                                class="h-4 flex-1 min-w-0 rounded-sm border border-[var(--color-border)] opacity-40"></span>
                                                            <% } %>
                                                                <% hbList.forEach(hb=> { %>
                                                                    <span
                                                                        class="h-4 flex-1 min-w-0 rounded-sm <%= hb.status === 'up' ? 'bg-[var(--color-primary)]' : 'bg-[var(--color-error)]' %> hover:opacity-80"
                                                                        title="<%= hb.ping %>ms"></span>
                                                                    <% }); %>
                                                                        <span
                                                                            class="h-4 flex-1 min-w-0 rounded-sm bg-[var(--color-secondary)] shadow-[0_0_8px_color-mix(in_srgb,var(--color-secondary)_80%,transparent)] animate-pulse"
                                                                            title="Checking..."></span>
                                                </div>

                                                <div class="flex items-center justify-between mt-2">
                                                    <span
                                                        class="inline-flex items-center gap-1.5 px-2 py-0.5 rounded-full text-[10px] font-bold bg-[color-mix(in_srgb,var(<%= chipColorVar %>)_15%,transparent)] text-[var(<%= chipColorVar %>)] border border-[color-mix(in_srgb,var(<%= chipColorVar %>)_30%,transparent)]">
                                                        <span
                                                            class="w-1.5 h-1.5 rounded-full bg-[var(<%= chipColorVar %>)]"></span>
                                                        <%= chipLabel %>
                                                    </span>
                                                    <span
                                                        class="text-[10px] font-mono text-[var(--color-text-secondary)] uppercase px-1.5 py-0.5 rounded bg-[var(--color-bg-surface)] border border-[var(--color-border)] shrink-0">
                                                        <%= site.type %>
                                                    </span>
                                                </div>
                                            </div>
                                            <% }); %>
                                </div>

                            </div>
                        </aside>

                        <!-- RIGHT MAIN COLUMN: Card-Based Dashboard Details Pane -->
                        <section class="lg:col-span-8 xl:col-span-9 space-y-6">

                            <!-- Active Monitor Header Card -->
                            <div
                                class="bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)] border border-[color-mix(in_srgb,var(--color-border)_80%,transparent)] rounded-2xl p-6 shadow-xl space-y-6">
                                <div class="flex flex-col md:flex-row md:items-center justify-between gap-4">
                                    <div>
                                        <div class="flex items-baseline space-x-2">
                                            <h1 id="activeName"
                                                class="text-3xl font-extrabold text-[var(--color-text-primary)] tracking-tight">
                                                -
                                            </h1>
                                            <span id="activeId"
                                                class="text-sm font-semibold text-[var(--color-text-muted)]">#0</span>
                                            <span id="activeTypeBadge"
                                                class="text-xs font-mono font-semibold px-2 py-0.5 rounded-full bg-[var(--color-bg-surface)] text-[var(--color-primary)] border border-[var(--color-border)] uppercase">
                                                -
                                            </span>
                                        </div>
                                        <a id="activeUrl" href="#" target="_blank" rel="noopener noreferrer"
                                            class="inline-flex items-center text-sm font-medium text-[var(--color-primary)] hover:text-[var(--color-primary)] underline underline-offset-4 mt-1 space-x-1">
                                            <span id="activeUrlText">-</span>
                                            <svg class="w-4 h-4 inline" fill="none" stroke="currentColor"
                                                viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                                    d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
                                            </svg>
                                        </a>
                                    </div>

                                    <div class="shrink-0">
                                        <div id="bigStatusBadge"
                                            class="px-8 py-3 rounded-full bg-[var(--color-primary)] text-[var(--color-bg-main)] font-black text-2xl tracking-wide shadow-lg shadow-[color-mix(in_srgb,var(--color-primary)_30%,transparent)] flex items-center justify-center">
                                            Up
                                        </div>
                                    </div>
                                </div>

                                <!-- Heartbeat Sequence -->
                                <div class="pt-2 space-y-2">
                                    <div id="activeHeartbeatBar"
                                        class="flex items-center space-x-1 w-full overflow-hidden py-1">
                                    </div>
                                    <p id="activeIntervalText"
                                        class="text-xs text-[var(--color-text-secondary)] font-medium">Check every 60
                                        seconds</p>
                                </div>
                            </div>

                            <!-- VPS PERFORMANCE CARD (shown only for 'vps' type monitors) -->
                            <div id="vpsPerformanceCard" class="hidden space-y-5">
                                <div class="flex items-center justify-between flex-wrap gap-3">
                                    <h2 class="text-base font-bold text-[var(--color-text-primary)] flex items-center gap-2">
                                        <svg class="w-5 h-5 text-[var(--color-primary)]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 3v2m6-2v2M9 19v2m6-2v2M5 9H3m2 6H3m18-6h-2m2 6h-2M7 19h10a2 2 0 002-2V7a2 2 0 00-2-2H7a2 2 0 00-2 2v10a2 2 0 002 2zM9 9h6v6H9V9z" />
                                        </svg>
                                        <span>VPS Performance</span>
                                    </h2>
                                    <div class="flex items-center gap-2">
                                        <span id="vpsLastSeen" class="text-[11px] font-mono text-[var(--color-text-muted)]"></span>
                                        <button type="button" onclick="showVpsInstallModal()"
                                            class="text-xs font-bold px-3 py-1.5 rounded-lg bg-[var(--color-bg-main)] border border-[var(--color-border)] text-[var(--color-text-secondary)] hover:text-[var(--color-primary)] hover:border-[var(--color-primary)] transition-colors">
                                            Agent Install Command
                                        </button>
                                    </div>
                                </div>

                                <div id="vpsMetricsBody" class="space-y-5">
                                        <div class="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-4 gap-4">
                                            <!-- CPU pie card -->
                                            <div class="bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)] border border-[color-mix(in_srgb,var(--color-border)_80%,transparent)] rounded-2xl p-5 shadow-xl flex flex-col items-center text-center space-y-3">
                                                <div class="flex items-center space-x-2 self-start">
                                                    <span class="w-2.5 h-2.5 rounded-full bg-[var(--color-primary)] inline-block shadow-[0_0_8px_color-mix(in_srgb,var(--color-primary)_80%,transparent)]"></span>
                                                    <h4 class="text-xs font-bold text-[var(--color-text-secondary)] uppercase tracking-wide">CPU</h4>
                                                </div>
                                                <div class="relative h-28 w-28">
                                                    <canvas id="vpsCpuChart"></canvas>
                                                </div>
                                                <p id="vpsLoadText" class="text-[10px] text-[var(--color-text-muted)] font-mono"></p>
                                            </div>
                                            <!-- RAM pie card -->
                                            <div class="bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)] border border-[color-mix(in_srgb,var(--color-border)_80%,transparent)] rounded-2xl p-5 shadow-xl flex flex-col items-center text-center space-y-3">
                                                <div class="flex items-center space-x-2 self-start">
                                                    <span class="w-2.5 h-2.5 rounded-full bg-[var(--color-secondary)] inline-block shadow-[0_0_8px_color-mix(in_srgb,var(--color-secondary)_80%,transparent)]"></span>
                                                    <h4 class="text-xs font-bold text-[var(--color-text-secondary)] uppercase tracking-wide">RAM</h4>
                                                </div>
                                                <div class="relative h-28 w-28">
                                                    <canvas id="vpsRamChart"></canvas>
                                                </div>
                                                <p id="vpsSwapText" class="text-[10px] text-[var(--color-text-muted)] font-mono"></p>
                                            </div>
                                            <!-- Storage pie card -->
                                            <div class="bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)] border border-[color-mix(in_srgb,var(--color-border)_80%,transparent)] rounded-2xl p-5 shadow-xl flex flex-col items-center text-center space-y-3">
                                                <div class="flex items-center space-x-2 self-start">
                                                    <span class="w-2.5 h-2.5 rounded-full bg-[var(--color-warning)] inline-block shadow-[0_0_8px_color-mix(in_srgb,var(--color-warning)_80%,transparent)]"></span>
                                                    <h4 class="text-xs font-bold text-[var(--color-text-secondary)] uppercase tracking-wide" id="vpsStorageLabel">Storage</h4>
                                                </div>
                                                <div class="relative h-28 w-28">
                                                    <canvas id="vpsStorageChart"></canvas>
                                                </div>
                                                <p id="vpsStorageText" class="text-[10px] text-[var(--color-text-muted)] font-mono"></p>
                                            </div>
                                            <!-- Uptime / network stat card -->
                                            <div class="bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)] border border-[color-mix(in_srgb,var(--color-border)_80%,transparent)] rounded-2xl p-5 shadow-xl flex flex-col items-center justify-center text-center space-y-3">
                                                <div class="flex items-center space-x-2 self-start">
                                                    <span class="w-2.5 h-2.5 rounded-full bg-[var(--color-text-secondary)] inline-block"></span>
                                                    <h4 class="text-xs font-bold text-[var(--color-text-secondary)] uppercase tracking-wide">Uptime</h4>
                                                </div>
                                                <div class="h-28 w-full flex items-center justify-center">
                                                    <span id="vpsUptimeText" class="text-3xl font-black text-[var(--color-text-primary)] font-mono">-</span>
                                                </div>
                                                <p id="vpsNetText" class="text-[10px] text-[var(--color-text-muted)] font-mono"></p>
                                            </div>
                                        </div>

                                        <div id="vpsDisksExtra" class="hidden">
                                            <h4 class="text-xs font-bold text-[var(--color-text-secondary)] uppercase tracking-wide mb-2">Other Mounts</h4>
                                            <div id="vpsDisksList" class="space-y-3"></div>
                                        </div>

                                        <!-- CPU & RAM history card -->
                                        <div class="bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)] border border-[color-mix(in_srgb,var(--color-border)_80%,transparent)] rounded-2xl p-5 shadow-xl">
                                            <h4 class="text-xs font-bold text-[var(--color-text-secondary)] uppercase tracking-wide mb-2">Performance History</h4>
                                            <div class="relative h-72 w-full pt-2">
                                                <canvas id="vpsHistoryChart"></canvas>
                                            </div>
                                        </div>

                                        <!-- Nginx: error.log / access.log cards -->
                                        <div id="vpsNginxCards" class="hidden grid grid-cols-1 lg:grid-cols-2 gap-4">
                                            <div class="bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)] border border-[color-mix(in_srgb,var(--color-border)_80%,transparent)] rounded-2xl p-5 shadow-xl space-y-3">
                                                <div class="flex items-center justify-between">
                                                    <div class="flex items-center space-x-2">
                                                        <span class="w-2.5 h-2.5 rounded-full bg-[var(--color-error)] inline-block shadow-[0_0_8px_color-mix(in_srgb,var(--color-error)_80%,transparent)]"></span>
                                                        <h5 class="text-xs font-bold text-[var(--color-text-secondary)] uppercase tracking-wide">Nginx Error Log</h5>
                                                    </div>
                                                    <span class="text-[10px] text-[var(--color-text-muted)] font-mono">Active: <span id="vpsNginxActive" class="text-[var(--color-text-primary)] font-bold">-</span> · Size: <span id="vpsNginxErrorSize" class="text-[var(--color-text-primary)] font-bold">-</span></span>
                                                </div>
                                                <div id="vpsNginxErrors"
                                                    class="max-h-48 overflow-y-auto overflow-x-auto text-[11px] font-mono text-[var(--color-error)] bg-[var(--color-bg-main)] border border-[var(--color-border)] rounded-xl p-3 space-y-1"></div>
                                            </div>
                                            <div class="bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)] border border-[color-mix(in_srgb,var(--color-border)_80%,transparent)] rounded-2xl p-5 shadow-xl space-y-3">
                                                <div class="flex items-center justify-between">
                                                    <div class="flex items-center space-x-2">
                                                        <span class="w-2.5 h-2.5 rounded-full bg-[var(--color-primary)] inline-block shadow-[0_0_8px_color-mix(in_srgb,var(--color-primary)_80%,transparent)]"></span>
                                                        <h5 class="text-xs font-bold text-[var(--color-text-secondary)] uppercase tracking-wide">Nginx Access Log</h5>
                                                    </div>
                                                    <span class="text-[10px] text-[var(--color-text-muted)] font-mono">Requests: <span id="vpsNginxRequests" class="text-[var(--color-text-primary)] font-bold">-</span> · Size: <span id="vpsNginxAccessSize" class="text-[var(--color-text-primary)] font-bold">-</span></span>
                                                </div>
                                                <div id="vpsNginxAccess"
                                                    class="max-h-48 overflow-y-auto overflow-x-auto text-[11px] font-mono text-[var(--color-text-secondary)] bg-[var(--color-bg-main)] border border-[var(--color-border)] rounded-xl p-3 space-y-1"></div>
                                            </div>
                                        </div>
                                </div>
                            </div>
                            <!-- MOBILE ANALYTICS CARD (shown only for 'mobile' type monitors) -->
                            <div id="mobileAnalyticsCard" class="hidden space-y-5">
                                <div class="flex items-center justify-between flex-wrap gap-3">
                                    <h2 class="text-base font-bold text-[var(--color-text-primary)] flex items-center gap-2">
                                        <svg class="w-5 h-5 text-[var(--color-primary)]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 18h.01M8 21h8a2 2 0 002-2V5a2 2 0 00-2-2H8a2 2 0 00-2 2v14a2 2 0 002 2z" />
                                        </svg>
                                        <span>Mobile Analytics</span>
                                    </h2>
                                    <button type="button" onclick="showMobileInstallModal()"
                                        class="text-xs font-bold px-3 py-1.5 rounded-lg bg-[var(--color-bg-main)] border border-[var(--color-border)] text-[var(--color-text-secondary)] hover:text-[var(--color-primary)] hover:border-[var(--color-primary)] transition-colors">
                                        Ingest URL &amp; Token
                                    </button>
                                </div>

                                <div class="grid grid-cols-2 sm:grid-cols-5 gap-4">
                                    <div class="bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)] border border-[color-mix(in_srgb,var(--color-border)_80%,transparent)] rounded-2xl p-4 text-center space-y-1">
                                        <p class="text-[10px] font-bold text-[var(--color-text-secondary)] uppercase tracking-wide">Active Today</p>
                                        <p id="mobileDau" class="text-xl font-black text-[var(--color-text-primary)]">-</p>
                                    </div>
                                    <div class="bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)] border border-[color-mix(in_srgb,var(--color-border)_80%,transparent)] rounded-2xl p-4 text-center space-y-1">
                                        <p class="text-[10px] font-bold text-[var(--color-text-secondary)] uppercase tracking-wide">Active This Week</p>
                                        <p id="mobileWau" class="text-xl font-black text-[var(--color-text-primary)]">-</p>
                                    </div>
                                    <div class="bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)] border border-[color-mix(in_srgb,var(--color-border)_80%,transparent)] rounded-2xl p-4 text-center space-y-1">
                                        <p class="text-[10px] font-bold text-[var(--color-text-secondary)] uppercase tracking-wide">Active This Month</p>
                                        <p id="mobileMau" class="text-xl font-black text-[var(--color-text-primary)]">-</p>
                                    </div>
                                    <div class="bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)] border border-[color-mix(in_srgb,var(--color-border)_80%,transparent)] rounded-2xl p-4 text-center space-y-1">
                                        <p class="text-[10px] font-bold text-[var(--color-text-secondary)] uppercase tracking-wide">Crash-Free Rate</p>
                                        <p id="mobileCrashFreeRate" class="text-xl font-black text-[var(--color-primary)]">-</p>
                                    </div>
                                    <div class="bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)] border border-[color-mix(in_srgb,var(--color-border)_80%,transparent)] rounded-2xl p-4 text-center space-y-1">
                                        <p class="text-[10px] font-bold text-[var(--color-text-secondary)] uppercase tracking-wide">Sessions (window)</p>
                                        <p id="mobileSessions" class="text-xl font-black text-[var(--color-text-primary)]">-</p>
                                    </div>
                                </div>

                                <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
                                    <!-- ACTIVE USERS PER DAY CHART CARD -->
                                    <div class="lg:col-span-2 bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)] border border-[color-mix(in_srgb,var(--color-border)_80%,transparent)] rounded-2xl p-6 shadow-xl space-y-4">
                                        <div class="flex items-center justify-between">
                                            <div class="flex items-center space-x-2">
                                                <span class="w-3 h-3 rounded bg-[var(--color-primary)] inline-block shadow-[0_0_8px_color-mix(in_srgb,var(--color-primary)_80%,transparent)]"></span>
                                                <h2 class="text-base font-bold text-[var(--color-text-primary)]">Active Users Per Day</h2>
                                            </div>

                                            <div class="relative">
                                                <select id="mobileDauRangeFilter" onchange="fetchAndRenderMobileAnalytics(monitors.find(x => x.id === activeId))"
                                                    class="appearance-none bg-[color-mix(in_srgb,var(--color-bg-main)_80%,transparent)] border border-[var(--color-border)] rounded-xl px-3.5 py-1.5 pr-8 text-xs font-medium text-[var(--color-text-secondary)] focus:outline-none focus:border-[color-mix(in_srgb,var(--color-primary)_50%,transparent)] cursor-pointer">
                                                    <option value="7d">7 Days</option>
                                                    <option value="14d">14 Days</option>
                                                    <option value="30d">30 Days</option>
                                                </select>
                                                <svg class="w-3.5 h-3.5 text-[var(--color-text-secondary)] absolute right-2.5 top-2.5 pointer-events-none"
                                                    fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                                        d="M19 9l-7 7-7-7" />
                                                </svg>
                                            </div>
                                        </div>

                                        <div class="relative h-64 w-full pt-2">
                                            <canvas id="mobileDauChart"></canvas>
                                        </div>
                                    </div>

                                    <!-- CRASH-FREE VS CRASHED PIE CHART CARD -->
                                    <div class="lg:col-span-1 bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)] border border-[color-mix(in_srgb,var(--color-border)_80%,transparent)] rounded-2xl p-6 shadow-xl space-y-4 flex flex-col">
                                        <div class="flex items-center space-x-2">
                                            <span class="w-3 h-3 rounded bg-[var(--color-primary)] inline-block shadow-[0_0_8px_color-mix(in_srgb,var(--color-primary)_80%,transparent)]"></span>
                                            <h2 class="text-base font-bold text-[var(--color-text-primary)]">Crash-Free vs Crashed</h2>
                                        </div>

                                        <div class="relative h-64 w-full pt-2 flex items-center justify-center">
                                            <canvas id="mobileCrashPieChart"></canvas>
                                        </div>

                                        <div class="flex items-center justify-center space-x-4 text-xs font-medium">
                                            <div class="flex items-center space-x-1.5">
                                                <span class="w-2.5 h-2.5 rounded-full bg-[var(--color-primary)]"></span>
                                                <span class="text-[var(--color-text-secondary)]">Crash-Free</span>
                                                <span id="pieCrashFreeValue"
                                                    class="text-[var(--color-text-primary)] font-bold">-</span>
                                            </div>
                                            <div class="flex items-center space-x-1.5">
                                                <span class="w-2.5 h-2.5 rounded-full bg-[var(--color-error)]"></span>
                                                <span class="text-[var(--color-text-secondary)]">Crashed</span>
                                                <span id="pieCrashedValue"
                                                    class="text-[var(--color-text-primary)] font-bold">-</span>
                                            </div>
                                        </div>
                                    </div>
                                </div>

                                <div class="grid grid-cols-1 lg:grid-cols-2 gap-4">
                                    <div class="lg:col-span-2 bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)] border border-[color-mix(in_srgb,var(--color-border)_80%,transparent)] rounded-2xl p-5 shadow-xl space-y-3">
                                        <h4 class="text-xs font-bold text-[var(--color-text-secondary)] uppercase tracking-wide flex items-center gap-1.5">
                                            <svg class="w-3.5 h-3.5 text-[var(--color-error)]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v4m0 4h.01M10.29 3.86L1.82 18a2 2 0 001.71 3h16.94a2 2 0 001.71-3L13.71 3.86a2 2 0 00-3.42 0z" />
                                            </svg>
                                            <span>Crash Issues</span>
                                        </h4>
                                        <div id="mobileCrashIssues" class="max-h-64 overflow-y-auto text-[11px] font-mono text-[var(--color-text-secondary)] space-y-1">
                                            <p class="text-[var(--color-text-muted)]">No crashes reported yet.</p>
                                        </div>
                                    </div>
                                    <div class="bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)] border border-[color-mix(in_srgb,var(--color-border)_80%,transparent)] rounded-2xl p-5 shadow-xl space-y-3">
                                        <h4 class="text-xs font-bold text-[var(--color-text-secondary)] uppercase tracking-wide flex items-center gap-1.5">
                                            <svg class="w-3.5 h-3.5 text-[var(--color-primary)]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 2L3 14h9l-1 8 10-12h-9l1-8z" />
                                            </svg>
                                            <span>Top Custom Events</span>
                                        </h4>
                                        <div id="mobileCustomEvents" class="max-h-64 overflow-y-auto text-[11px] font-mono text-[var(--color-text-secondary)] space-y-1">
                                            <p class="text-[var(--color-text-muted)]">No custom events yet.</p>
                                        </div>
                                    </div>
                                    <div class="bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)] border border-[color-mix(in_srgb,var(--color-border)_80%,transparent)] rounded-2xl p-5 shadow-xl space-y-3">
                                        <h4 class="text-xs font-bold text-[var(--color-text-secondary)] uppercase tracking-wide flex items-center gap-1.5">
                                            <svg class="w-3.5 h-3.5 text-[var(--color-primary)]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.59 13.41L11 3.83A2 2 0 009.59 3H4a1 1 0 00-1 1v5.59a2 2 0 00.59 1.41l9.58 9.58a2 2 0 002.82 0l6.6-6.6a2 2 0 000-2.82z" />
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 7h.01" />
                                            </svg>
                                            <span>App Version Adoption</span>
                                        </h4>
                                        <div id="mobileVersionBreakdown" class="max-h-48 overflow-y-auto text-[11px] font-mono text-[var(--color-text-secondary)] space-y-1">
                                            <p class="text-[var(--color-text-muted)]">No data yet.</p>
                                        </div>
                                    </div>
                                    <div class="bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)] border border-[color-mix(in_srgb,var(--color-border)_80%,transparent)] rounded-2xl p-5 shadow-xl space-y-3">
                                        <h4 class="text-xs font-bold text-[var(--color-text-secondary)] uppercase tracking-wide flex items-center gap-1.5">
                                            <svg class="w-3.5 h-3.5 text-[var(--color-primary)]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 2H7a2 2 0 00-2 2v16a2 2 0 002 2h10a2 2 0 002-2V4a2 2 0 00-2-2z" />
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 18h.01" />
                                            </svg>
                                            <span>OS Breakdown</span>
                                        </h4>
                                        <div id="mobileOsBreakdown" class="max-h-48 overflow-y-auto text-[11px] font-mono text-[var(--color-text-secondary)] space-y-1">
                                            <p class="text-[var(--color-text-muted)]">No data yet.</p>
                                        </div>
                                    </div>
                                    <div class="bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)] border border-[color-mix(in_srgb,var(--color-border)_80%,transparent)] rounded-2xl p-5 shadow-xl space-y-3">
                                        <h4 class="text-xs font-bold text-[var(--color-text-secondary)] uppercase tracking-wide flex items-center gap-1.5">
                                            <svg class="w-3.5 h-3.5 text-[var(--color-primary)]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 21a9 9 0 100-18 9 9 0 000 18z" />
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12h18M12 3a14.5 14.5 0 010 18M12 3a14.5 14.5 0 000 18" />
                                            </svg>
                                            <span>Region Breakdown</span>
                                        </h4>
                                        <div id="mobileRegionBreakdown" class="max-h-48 overflow-y-auto text-[11px] font-mono text-[var(--color-text-secondary)] space-y-1">
                                            <p class="text-[var(--color-text-muted)]">No data yet.</p>
                                        </div>
                                    </div>
                                </div>
                                <!-- Per-event detail (screen views, crashes, sessions, device info) lives in the
                                     Heartbeat Event Logs table below, not here — see renderLogsTable()'s mobile
                                     branch, which merges tbl_mobile_events into that same table for mobile monitors. -->
                            </div>

                            <!-- KEY METRICS GRID CARD -->
                            <div id="httpMetricsCard"
                                class="bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)] border border-[color-mix(in_srgb,var(--color-border)_80%,transparent)] rounded-2xl p-6 shadow-xl">
                                <div
                                    class="grid grid-cols-2 sm:grid-cols-4 lg:grid-cols-7 gap-4 text-center divide-x-0 sm:divide-x divide-[color-mix(in_srgb,var(--color-bg-surface)_80%,transparent)]">
                                    <div class="p-2 space-y-1">
                                        <div class="text-xs font-semibold text-[var(--color-text-secondary)]">Response
                                        </div>
                                        <div class="text-[11px] text-[var(--color-text-muted)]">(Current)</div>
                                        <div id="metricCurrent"
                                            class="text-lg font-bold text-[var(--color-text-primary)] font-mono underline decoration-[color-mix(in_srgb,var(--color-primary)_60%,transparent)] underline-offset-4">
                                            -</div>
                                    </div>
                                    <div class="p-2 space-y-1">
                                        <div class="text-xs font-semibold text-[var(--color-text-secondary)]">Avg.
                                            Response
                                        </div>
                                        <div class="text-[11px] text-[var(--color-text-muted)]">(24 hours)</div>
                                        <div id="metricAvg24h"
                                            class="text-lg font-bold text-[var(--color-text-primary)] font-mono">-</div>
                                    </div>
                                    <div class="p-2 space-y-1">
                                        <div class="text-xs font-semibold text-[var(--color-text-secondary)]">Uptime
                                        </div>
                                        <div class="text-[11px] text-[var(--color-text-muted)]">(24 hours)</div>
                                        <div id="metricUptime24h"
                                            class="text-lg font-bold text-[var(--color-primary)] font-mono">-</div>
                                    </div>
                                    <div class="p-2 space-y-1">
                                        <div class="text-xs font-semibold text-[var(--color-text-secondary)]">Uptime
                                        </div>
                                        <div class="text-[11px] text-[var(--color-text-muted)]">(30 days)</div>
                                        <div id="metricUptime30d"
                                            class="text-lg font-bold text-[var(--color-primary)] font-mono">-</div>
                                    </div>
                                    <div class="p-2 space-y-1">
                                        <div class="text-xs font-semibold text-[var(--color-text-secondary)]">Uptime
                                        </div>
                                        <div class="text-[11px] text-[var(--color-text-muted)]">(1 year)</div>
                                        <div id="metricUptime1y"
                                            class="text-lg font-bold text-[var(--color-primary)] font-mono">-</div>
                                    </div>
                                    <div class="p-2 space-y-1">
                                        <div class="text-xs font-semibold text-[var(--color-text-secondary)]">Cert Exp.
                                        </div>
                                        <div id="metricCertSub"
                                            class="text-[10px] text-[var(--color-text-muted)] font-mono">-</div>
                                        <div id="metricCertDays"
                                            class="text-sm font-bold text-[var(--color-secondary)] font-mono underline underline-offset-2">
                                            -</div>
                                    </div>
                                    <div class="p-2 space-y-1">
                                        <div class="text-xs font-semibold text-[var(--color-text-secondary)]">Domain
                                            Exp.
                                        </div>
                                        <div id="metricDomainSub"
                                            class="text-[10px] text-[var(--color-text-muted)] font-mono">-</div>
                                        <div id="metricDomainDays"
                                            class="text-sm font-bold text-[var(--color-text-primary)] font-mono">-
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <!-- RESPONSE TIME + UPTIME/DOWNTIME CHART ROW -->
                            <div id="responseChartRow" class="grid grid-cols-1 lg:grid-cols-3 gap-6">

                                <!-- RESPONSE TIME ANALYTICS CHART CARD -->
                                <div
                                    class="lg:col-span-2 bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)] border border-[color-mix(in_srgb,var(--color-border)_80%,transparent)] rounded-2xl p-6 shadow-xl space-y-4">
                                    <div class="flex items-center justify-between">
                                        <div class="flex items-center space-x-2">
                                            <span id="chartLegendDot"
                                                class="w-3 h-3 rounded bg-[var(--color-primary)] inline-block shadow-[0_0_8px_color-mix(in_srgb,var(--color-primary)_80%,transparent)]"></span>
                                            <h2 id="chartLegendTitle"
                                                class="text-base font-bold text-[var(--color-text-primary)]">Avg Ping
                                            </h2>
                                        </div>

                                        <div class="relative">
                                            <select id="chartTimeFilter" onchange="updateChartFilter()"
                                                class="appearance-none bg-[color-mix(in_srgb,var(--color-bg-main)_80%,transparent)] border border-[var(--color-border)] rounded-xl px-3.5 py-1.5 pr-8 text-xs font-medium text-[var(--color-text-secondary)] focus:outline-none focus:border-[color-mix(in_srgb,var(--color-primary)_50%,transparent)] cursor-pointer">
                                                <option value="Recent">Recent ▾</option>
                                                <option value="1h">1 Hour</option>
                                                <option value="6h">6 Hours</option>
                                                <option value="24h">24 Hours</option>
                                                <option value="7d">7 Days</option>
                                            </select>
                                            <svg class="w-3.5 h-3.5 text-[var(--color-text-secondary)] absolute right-2.5 top-2.5 pointer-events-none"
                                                fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                                    d="M19 9l-7 7-7-7" />
                                            </svg>
                                        </div>
                                    </div>

                                    <div class="relative h-64 w-full pt-2">
                                        <canvas id="responseChart"></canvas>
                                    </div>
                                </div>

                                <!-- UPTIME VS DOWNTIME PIE CHART CARD -->
                                <div
                                    class="lg:col-span-1 bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)] border border-[color-mix(in_srgb,var(--color-border)_80%,transparent)] rounded-2xl p-6 shadow-xl space-y-4 flex flex-col">
                                    <div class="flex items-center space-x-2">
                                        <span
                                            class="w-3 h-3 rounded bg-[var(--color-primary)] inline-block shadow-[0_0_8px_color-mix(in_srgb,var(--color-primary)_80%,transparent)]"></span>
                                        <h2 class="text-base font-bold text-[var(--color-text-primary)]">Uptime vs
                                            Downtime
                                        </h2>
                                    </div>

                                    <div class="relative h-64 w-full pt-2 flex items-center justify-center">
                                        <canvas id="uptimePieChart"></canvas>
                                    </div>

                                    <div class="flex items-center justify-center space-x-4 text-xs font-medium">
                                        <div class="flex items-center space-x-1.5">
                                            <span class="w-2.5 h-2.5 rounded-full bg-[var(--color-primary)]"></span>
                                            <span class="text-[var(--color-text-secondary)]">Uptime</span>
                                            <span id="pieUptimeValue"
                                                class="text-[var(--color-text-primary)] font-bold">-</span>
                                        </div>
                                        <div class="flex items-center space-x-1.5">
                                            <span class="w-2.5 h-2.5 rounded-full bg-[var(--color-error)]"></span>
                                            <span class="text-[var(--color-text-secondary)]">Downtime</span>
                                            <span id="pieDowntimeValue"
                                                class="text-[var(--color-text-primary)] font-bold">-</span>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <!-- HEARTBEAT EVENT LOGS CARD -->
                            <div
                                class="bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)] border border-[color-mix(in_srgb,var(--color-border)_80%,transparent)] rounded-2xl p-6 shadow-xl space-y-4">
                                <div class="flex items-center justify-between">
                                    <h3
                                        class="text-base font-bold text-[var(--color-text-primary)] flex items-center space-x-2">
                                        <svg class="w-5 h-5 text-[var(--color-primary)]" fill="none"
                                            stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                                d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                                        </svg>
                                        <span>Heartbeat Event Logs</span>
                                    </h3>
                                    <div class="flex items-center space-x-3 flex-wrap gap-y-2">
                                        <span class="text-xs text-[var(--color-text-muted)] font-mono">Live checks
                                            active</span>
                                        <div class="relative">
                                            <select id="logsStatusFilter" onchange="renderLogsTable()"
                                                class="appearance-none bg-[color-mix(in_srgb,var(--color-bg-main)_80%,transparent)] border border-[var(--color-border)] rounded-xl px-3 py-1.5 pr-7 text-xs font-medium text-[var(--color-text-secondary)] focus:outline-none focus:border-[color-mix(in_srgb,var(--color-primary)_50%,transparent)] cursor-pointer">
                                                <option value="ALL" selected>All statuses</option>
                                                <option value="200 OK">Up (200 OK)</option>
                                                <option value="ERROR">Down (Error)</option>
                                            </select>
                                            <svg class="w-3.5 h-3.5 text-[var(--color-text-secondary)] absolute right-2.5 top-2 pointer-events-none"
                                                fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                                    d="M19 9l-7 7-7-7" />
                                            </svg>
                                        </div>
                                        <input type="date" id="logsDateFilter" onchange="renderLogsTable()"
                                            class="bg-[color-mix(in_srgb,var(--color-bg-main)_80%,transparent)] border border-[var(--color-border)] rounded-xl px-3 py-1.5 text-xs font-medium text-[var(--color-text-secondary)] focus:outline-none focus:border-[color-mix(in_srgb,var(--color-primary)_50%,transparent)] cursor-pointer">
                                        <div class="relative">
                                            <select id="logsLimitFilter" onchange="renderLogsTable()"
                                                class="appearance-none bg-[color-mix(in_srgb,var(--color-bg-main)_80%,transparent)] border border-[var(--color-border)] rounded-xl px-3 py-1.5 pr-7 text-xs font-medium text-[var(--color-text-secondary)] focus:outline-none focus:border-[color-mix(in_srgb,var(--color-primary)_50%,transparent)] cursor-pointer">
                                                <option value="10">Show 10</option>
                                                <option value="20" selected>Show 20</option>
                                                <option value="50">Show 50</option>
                                            </select>
                                            <svg class="w-3.5 h-3.5 text-[var(--color-text-secondary)] absolute right-2.5 top-2 pointer-events-none"
                                                fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                                    d="M19 9l-7 7-7-7" />
                                            </svg>
                                        </div>
                                    </div>
                                </div>

                                <div class="overflow-x-auto">
                                    <table class="w-full text-left text-xs text-[var(--color-text-secondary)]">
                                        <thead
                                            class="bg-[color-mix(in_srgb,var(--color-bg-main)_60%,transparent)] text-[var(--color-text-secondary)] font-semibold border-b border-[var(--color-bg-surface)]">
                                            <tr id="logsTableHeadRow">
                                                <th class="py-2.5 px-4 rounded-l-xl">Timestamp</th>
                                                <th class="py-2.5 px-4">Status</th>
                                                <th class="py-2.5 px-4">Latency</th>
                                                <th class="py-2.5 px-4">Details</th>
                                                <th class="py-2.5 px-4 rounded-r-xl">Response</th>
                                            </tr>
                                        </thead>
                                        <tbody id="logsTableBody"
                                            class="divide-y divide-[color-mix(in_srgb,var(--color-bg-surface)_40%,transparent)] font-mono">
                                        </tbody>
                                    </table>
                                </div>
                            </div>

                            <!-- MONITOR ACTIONS SECTION (bottom of page) -->
                            <div
                                class="mt-6 bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)] border border-[color-mix(in_srgb,var(--color-border)_80%,transparent)] rounded-2xl p-4 shadow-xl">
                                <div class="flex items-stretch justify-between gap-3">
                                    <button onclick="togglePauseActive()" id="btnPause"
                                        class="flex-1 h-12 rounded-xl bg-[var(--color-bg-surface)] hover:bg-[var(--color-border)] text-[var(--color-text-primary)] border border-[var(--color-border)] text-sm font-semibold flex items-center justify-center space-x-2 transition-all">
                                        <svg class="w-4 h-4 text-[var(--color-warning)]" fill="none"
                                            stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                                d="M10 9v6m4-6v6m7-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                                        </svg>
                                        <span id="pauseBtnText">Pause</span>
                                    </button>
                                    <button onclick="editActive()"
                                        class="flex-1 h-12 rounded-xl bg-[var(--color-bg-surface)] hover:bg-[var(--color-border)] text-[var(--color-text-primary)] border border-[var(--color-border)] text-sm font-semibold flex items-center justify-center space-x-2 transition-all">
                                        <svg class="w-4 h-4 text-[var(--color-primary)]" fill="none"
                                            stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                                d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z" />
                                        </svg>
                                        <span>Edit</span>
                                    </button>
                                    <button onclick="resetActive()"
                                        class="flex-1 h-12 rounded-xl bg-[var(--color-bg-surface)] hover:bg-[var(--color-border)] text-[var(--color-text-primary)] border border-[var(--color-border)] text-sm font-semibold flex items-center justify-center space-x-2 transition-all">
                                        <svg class="w-4 h-4 text-[var(--color-secondary)]" fill="none"
                                            stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                                d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15" />
                                        </svg>
                                        <span>Reset</span>
                                    </button>
                                    <button onclick="cloneActive()"
                                        class="flex-1 h-12 rounded-xl bg-[var(--color-bg-surface)] hover:bg-[var(--color-border)] text-[var(--color-text-primary)] border border-[var(--color-border)] text-sm font-semibold flex items-center justify-center space-x-2 transition-all">
                                        <svg class="w-4 h-4 text-[var(--color-secondary)]" fill="none"
                                            stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                                d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z" />
                                        </svg>
                                        <span>Clone</span>
                                    </button>
                                    <button onclick="deleteActive()"
                                        class="flex-1 h-12 rounded-xl bg-[color-mix(in_srgb,var(--color-error)_10%,transparent)] hover:bg-[color-mix(in_srgb,var(--color-error)_20%,transparent)] text-[var(--color-error)] border border-[color-mix(in_srgb,var(--color-error)_30%,transparent)] text-sm font-semibold flex items-center justify-center space-x-2 transition-all">
                                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                                d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
                                        </svg>
                                        <span>Delete</span>
                                    </button>
                                </div>
                            </div>

                        </section>
                    </div>


                    <% } %>

        </main>



        <!-- DELETE CONFIRMATION MODAL -->
        <div id="deleteConfirmModal"
            class="fixed inset-0 z-50 bg-[color-mix(in_srgb,var(--color-bg-main)_85%,transparent)] backdrop-blur-md hidden flex items-center justify-center p-4">
            <div class="bg-[var(--color-bg-surface)] border border-[color-mix(in_srgb,var(--color-border)_80%,transparent)] rounded-2xl max-w-sm w-full p-6 shadow-2xl space-y-5 transform transition-all scale-95 opacity-0"
                id="deleteConfirmContainer">
                <div class="flex items-center space-x-3">
                    <div
                        class="w-10 h-10 rounded-xl bg-[color-mix(in_srgb,var(--color-error)_15%,transparent)] border border-[color-mix(in_srgb,var(--color-error)_30%,transparent)] flex items-center justify-center shrink-0">
                        <svg class="w-5 h-5 text-[var(--color-error)]" fill="none" stroke="currentColor"
                            viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
                        </svg>
                    </div>
                    <div>
                        <h3 class="text-sm font-bold text-[var(--color-text-primary)]">Delete Monitor</h3>
                        <p class="text-xs text-[var(--color-text-secondary)]">This action cannot be undone.</p>
                    </div>
                </div>
                <p class="text-xs text-[var(--color-text-secondary)]">
                    Are you sure you want to delete <span id="deleteConfirmName"
                        class="font-semibold text-[var(--color-text-primary)]">this monitor</span>? All heartbeat
                    history and logs will be permanently removed.
                </p>
                <div class="flex items-center justify-end space-x-2 pt-1">
                    <button type="button" onclick="closeDeleteConfirmModal()"
                        class="px-4 py-2 rounded-xl bg-[var(--color-bg-main)] hover:bg-[var(--color-border)] text-[var(--color-text-secondary)] border border-[var(--color-border)] text-xs font-semibold transition-all">
                        Cancel
                    </button>
                    <button type="button" onclick="confirmDeleteActive()"
                        class="px-4 py-2 rounded-xl bg-[var(--color-error)] hover:opacity-90 text-white text-xs font-bold transition-all">
                        Delete
                    </button>
                </div>
            </div>
        </div>

        <!-- RESET CONFIRMATION MODAL -->
        <div id="resetConfirmModal"
            class="fixed inset-0 z-50 bg-[color-mix(in_srgb,var(--color-bg-main)_85%,transparent)] backdrop-blur-md hidden flex items-center justify-center p-4">
            <div class="bg-[var(--color-bg-surface)] border border-[color-mix(in_srgb,var(--color-border)_80%,transparent)] rounded-2xl max-w-sm w-full p-6 shadow-2xl space-y-5 transform transition-all scale-95 opacity-0"
                id="resetConfirmContainer">
                <div class="flex items-center space-x-3">
                    <div
                        class="w-10 h-10 rounded-xl bg-[color-mix(in_srgb,var(--color-secondary)_15%,transparent)] border border-[color-mix(in_srgb,var(--color-secondary)_30%,transparent)] flex items-center justify-center shrink-0">
                        <svg class="w-5 h-5 text-[var(--color-secondary)]" fill="none" stroke="currentColor"
                            viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15" />
                        </svg>
                    </div>
                    <div>
                        <h3 class="text-sm font-bold text-[var(--color-text-primary)]">Reset Monitor Data</h3>
                        <p class="text-xs text-[var(--color-text-secondary)]">This action cannot be undone.</p>
                    </div>
                </div>
                <p class="text-xs text-[var(--color-text-secondary)]">
                    Are you sure you want to reset all data for <span id="resetConfirmName"
                        class="font-semibold text-[var(--color-text-primary)]">this monitor</span>? All heartbeat
                    history, logs and analytics will be permanently removed. The monitor's settings (interval,
                    retries, etc.) will be kept.
                </p>
                <div class="flex items-center justify-end space-x-2 pt-1">
                    <button type="button" onclick="closeResetConfirmModal()"
                        class="px-4 py-2 rounded-xl bg-[var(--color-bg-main)] hover:bg-[var(--color-border)] text-[var(--color-text-secondary)] border border-[var(--color-border)] text-xs font-semibold transition-all">
                        Cancel
                    </button>
                    <button type="button" onclick="confirmResetActive()"
                        class="px-4 py-2 rounded-xl bg-[var(--color-secondary)] hover:opacity-90 text-white text-xs font-bold transition-all">
                        Reset
                    </button>
                </div>
            </div>
        </div>

        <!-- MOBILE APP INTEGRATION MODAL -->
        <div id="mobileInstallModal"
            class="fixed inset-0 z-50 bg-[color-mix(in_srgb,var(--color-bg-main)_85%,transparent)] backdrop-blur-md hidden flex items-center justify-center p-4">
            <div class="bg-[var(--color-bg-surface)] border border-[color-mix(in_srgb,var(--color-border)_80%,transparent)] rounded-2xl max-w-2xl w-full p-6 shadow-2xl space-y-5 transform transition-all scale-95 opacity-0 max-h-[85vh] overflow-y-auto"
                id="mobileInstallContainer">
                <div class="flex items-center justify-between">
                    <div class="flex items-center space-x-3">
                        <div
                            class="w-10 h-10 rounded-xl bg-[color-mix(in_srgb,var(--color-primary)_15%,transparent)] border border-[color-mix(in_srgb,var(--color-primary)_30%,transparent)] flex items-center justify-center shrink-0">
                            <svg class="w-5 h-5 text-[var(--color-primary)]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 18h.01M8 21h8a2 2 0 002-2V5a2 2 0 00-2-2H8a2 2 0 00-2 2v14a2 2 0 002 2z" />
                            </svg>
                        </div>
                        <div>
                            <h3 class="text-sm font-bold text-[var(--color-text-primary)]">Mobile App Integration</h3>
                            <p class="text-xs text-[var(--color-text-secondary)]">Send crash reports, custom events, and session data from your app.</p>
                        </div>
                    </div>
                    <button type="button" onclick="closeMobileInstallModal()"
                        class="p-1.5 rounded-lg hover:bg-[var(--color-bg-main)] text-[var(--color-text-muted)] hover:text-[var(--color-text-primary)] transition-colors shrink-0">
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                        </svg>
                    </button>
                </div>

                <div class="space-y-1.5">
                    <label class="text-[10px] font-bold uppercase tracking-wide text-[var(--color-text-muted)]">Ingest URL</label>
                    <div class="flex items-center gap-1.5 bg-[var(--color-bg-main)] border border-[var(--color-border)] rounded-xl px-3 py-2">
                        <code id="mobileInstallUrl" class="flex-1 text-xs font-mono text-[var(--color-text-primary)] truncate"></code>
                        <button type="button" onclick="copyTextToClipboard(document.getElementById('mobileInstallUrl').textContent, this, 'Ingest URL copied', 'Failed to copy')"
                            title="Copy" class="shrink-0 p-1 rounded-md hover:bg-[var(--color-bg-surface)] text-[var(--color-text-muted)] hover:text-[var(--color-text-primary)] transition-colors">
                            <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z" /></svg>
                        </button>
                    </div>
                </div>

                <div class="space-y-1.5">
                    <label class="text-[10px] font-bold uppercase tracking-wide text-[var(--color-text-muted)]">Mobile Token (Bearer) — keep this secret</label>
                    <div class="flex items-center gap-1.5 bg-[var(--color-bg-main)] border border-[var(--color-border)] rounded-xl px-3 py-2">
                        <code id="mobileInstallToken" class="flex-1 text-xs font-mono text-[var(--color-text-primary)] truncate"></code>
                        <button type="button" onclick="copyTextToClipboard(document.getElementById('mobileInstallToken').textContent, this, 'Token copied', 'Failed to copy')"
                            title="Copy" class="shrink-0 p-1 rounded-md hover:bg-[var(--color-bg-surface)] text-[var(--color-text-muted)] hover:text-[var(--color-text-primary)] transition-colors">
                            <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z" /></svg>
                        </button>
                    </div>
                </div>

                <div class="space-y-1.5">
                    <div class="flex items-center justify-between">
                        <label class="text-[10px] font-bold uppercase tracking-wide text-[var(--color-text-muted)]">Quick test (curl)</label>
                        <button type="button" onclick="copyTextToClipboard(document.getElementById('mobileInstallCurl').textContent, this, 'Copied', 'Failed to copy')"
                            title="Copy" class="shrink-0 p-1 rounded-md hover:bg-[var(--color-bg-main)] text-[var(--color-text-muted)] hover:text-[var(--color-text-primary)] transition-colors">
                            <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z" /></svg>
                        </button>
                    </div>
                    <pre id="mobileInstallCurl" class="text-[11px] font-mono bg-[var(--color-bg-main)] border border-[var(--color-border)] rounded-xl p-3 overflow-x-auto whitespace-pre text-[var(--color-text-secondary)]"></pre>
                </div>

                <div class="space-y-1.5">
                    <div class="flex items-center justify-between">
                        <label class="text-[10px] font-bold uppercase tracking-wide text-[var(--color-text-muted)]">React Native / Expo (fetch)</label>
                        <button type="button" onclick="copyTextToClipboard(document.getElementById('mobileInstallFetch').textContent, this, 'Copied', 'Failed to copy')"
                            title="Copy" class="shrink-0 p-1 rounded-md hover:bg-[var(--color-bg-main)] text-[var(--color-text-muted)] hover:text-[var(--color-text-primary)] transition-colors">
                            <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z" /></svg>
                        </button>
                    </div>
                    <pre id="mobileInstallFetch" class="text-[11px] font-mono bg-[var(--color-bg-main)] border border-[var(--color-border)] rounded-xl p-3 overflow-x-auto whitespace-pre text-[var(--color-text-secondary)]"></pre>
                </div>

                <p class="text-xs text-[var(--color-text-secondary)] bg-[color-mix(in_srgb,var(--color-primary)_8%,transparent)] border border-[color-mix(in_srgb,var(--color-primary)_20%,transparent)] rounded-xl p-3">
                    Building this into a React Native / Expo app rather than testing with curl? A ready-to-copy
                    reference implementation (device id, batching queue, crash handler, session + screen tracking)
                    lives in <code class="font-mono">mobile-monitoring/</code> in this repo — see
                    <code class="font-mono">docs/mobile-monitor-integration.md</code> for how to wire it in.
                </p>

                <div class="flex justify-end pt-1">
                    <button type="button" onclick="closeMobileInstallModal()"
                        class="px-4 py-2 rounded-xl bg-[var(--color-primary)] hover:opacity-90 text-white text-xs font-bold transition-all">
                        Done
                    </button>
                </div>
            </div>
        </div>

        <!-- Footer -->
        <footer
            class="border-t border-[var(--color-bg-surface)] py-4 text-center text-xs text-[var(--color-text-muted)]">
            Uptinger Engine • Real-Time Uptime Tracker<br>
            Powered by Emitlogic Technologies Private Limited
        </footer>

        <!-- Client Scripting Engine -->
        <script>
            let monitors = <%- jsonScript(sites) %>;
            // Populated by fetchAndRenderMobileAnalytics() and consumed by renderLogsTable()'s
            // mobile branch — the Heartbeat Event Logs table doubles as the mobile event feed
            // (screen views/crashes/sessions/device info) for 'mobile' type monitors.
            let mobileEventFeedCache = [];

            function getMonitorIdFromUrl() {
                const raw = new URLSearchParams(window.location.search).get('monitor');
                const id = raw ? parseInt(raw, 10) : NaN;
                return Number.isNaN(id) ? null : id;
            }

            function setMonitorIdInUrl(id) {
                const url = new URL(window.location.href);
                if (id) {
                    url.searchParams.set('monitor', id);
                } else {
                    url.searchParams.delete('monitor');
                }
                history.replaceState(null, '', url);
            }

            const urlMonitorId = getMonitorIdFromUrl();
            let activeId = (urlMonitorId && monitors.find(x => x.id === urlMonitorId))
                ? urlMonitorId
                : (monitors.length > 0 ? monitors[0].id : null);
            let chartInstance = null;
            let uptimePieChartInstance = null;

            const LIVE_REFRESH_INTERVAL_MS = 5000;
            let liveRefreshTimer = null;
            let liveRefreshInFlight = false;

            document.addEventListener('DOMContentLoaded', () => {
                initChart();
                if (activeId) {
                    setMonitorIdInUrl(activeId);
                    highlightActiveCard(!!urlMonitorId);
                    renderActiveMonitor(activeId);
                }
                startLiveRefresh();
                loadTagFilterOptions();
                loadTypeFilterOptions();
            });

            function loadTypeFilterOptions() {
                fetch('/api/monitor-types')
                    .then(r => r.json())
                    .then(res => {
                        if (!res.status || !res.data || !res.data.types) return;
                        const select = document.getElementById('typeFilter');
                        const current = select.value;
                        const allTypes = Object.values(res.data.types).flat();
                        select.innerHTML = '<option value="ALL">Type ▾</option>' +
                            allTypes.map(t => `<option value="${t.key}">${t.label}</option>`).join('');
                        if ([...select.options].some(o => o.value === current)) select.value = current;
                    })
                    .catch(() => { });
            }

            function loadTagFilterOptions() {
                fetch('/api/tags')
                    .then(r => r.json())
                    .then(res => {
                        if (!res.status || !res.data || !res.data.tags) return;
                        const select = document.getElementById('tagFilter');
                        const current = select.value;
                        select.innerHTML = '<option value="ALL">Tags ▾</option>' +
                            res.data.tags.map(t => `<option value="${t.name}">${t.name}</option>`).join('');
                        if ([...select.options].some(o => o.value === current)) select.value = current;
                    })
                    .catch(() => { });
            }

            function startLiveRefresh() {
                if (liveRefreshTimer) return;
                liveRefreshTimer = setInterval(refreshDashboardData, LIVE_REFRESH_INTERVAL_MS);
            }

            async function refreshDashboardData() {
                if (liveRefreshInFlight) return;
                liveRefreshInFlight = true;
                try {
                    const res = await fetch('/api/monitors/dashboard/live' + location.search, { headers: { 'Accept': 'application/json' } });
                    const json = await res.json();
                    if (!json || !json.status) return;

                    monitors = json.data.sites || [];

                    if (!monitors.find(x => x.id === activeId)) {
                        activeId = monitors.length > 0 ? monitors[0].id : null;
                        setMonitorIdInUrl(activeId);
                    }

                    renderMonitorsList();
                    if (activeId) {
                        renderActiveMonitor(activeId);
                    } else {
                        renderLogsTable();
                    }
                } catch (err) {
                    console.error('Live dashboard refresh failed', err);
                } finally {
                    liveRefreshInFlight = false;
                }
            }

            function buildMonitorCardHTML(site) {
                const hbCellCount = 24;
                const hbList = (site.heartbeats || []).slice(-hbCellCount);
                const hbEmptyCount = Math.max(0, hbCellCount - hbList.length);

                let hbHtml = '';
                for (let i = 0; i < hbEmptyCount; i++) {
                    hbHtml += `<span class="h-4 flex-1 min-w-0 rounded-sm border border-[var(--color-border)] opacity-40"></span>`;
                }
                hbList.forEach(hb => {
                    hbHtml += `<span class="h-4 flex-1 min-w-0 rounded-sm ${hb.status === 'up' ? 'bg-[var(--color-primary)]' : 'bg-[var(--color-error)]'} hover:opacity-80" title="${hb.ping}ms"></span>`;
                });
                hbHtml += `<span class="h-4 flex-1 min-w-0 rounded-sm bg-[var(--color-secondary)] shadow-[0_0_8px_color-mix(in_srgb,var(--color-secondary)_80%,transparent)] animate-pulse" title="Checking..."></span>`;

                const isPaused = site.is_paused;
                const isUp = !isPaused && site.status === 'ONLINE';
                const chipLabel = isPaused ? 'Paused' : (isUp ? 'Up' : 'Down');
                const chipColorVar = isPaused ? '--color-warning' : (isUp ? '--color-primary' : '--color-error');

                return `
                <div onclick="selectMonitor(${site.id})" id="monitor-card-${site.id}"
                    class="monitor-item group cursor-pointer bg-[color-mix(in_srgb,var(--color-bg-main)_40%,transparent)] hover:bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)] border border-[color-mix(in_srgb,var(--color-border)_70%,transparent)] rounded-xl p-3 transition-all duration-200 relative overflow-hidden">
                    <div class="flex items-center justify-between mb-2">
                        <span class="font-semibold text-sm text-[var(--color-text-primary)] group-hover:text-[var(--color-primary)] transition-colors truncate">${site.name}</span>
                    </div>
                    <div class="flex items-center space-x-1 w-full overflow-hidden">${hbHtml}</div>
                    <div class="flex items-center justify-between mt-2">
                        <span class="inline-flex items-center gap-1.5 px-2 py-0.5 rounded-full text-[10px] font-bold bg-[color-mix(in_srgb,var(${chipColorVar})_15%,transparent)] text-[var(${chipColorVar})] border border-[color-mix(in_srgb,var(${chipColorVar})_30%,transparent)]">
                            <span class="w-1.5 h-1.5 rounded-full bg-[var(${chipColorVar})]"></span>
                            ${chipLabel}
                        </span>
                        <span class="text-[10px] font-mono text-[var(--color-text-secondary)] uppercase px-1.5 py-0.5 rounded bg-[var(--color-bg-surface)] border border-[var(--color-border)] shrink-0">${site.type}</span>
                    </div>
                </div>`;
            }

            function monitorCardSignature(site) {
                return JSON.stringify({
                    name: site.name,
                    type: site.type,
                    is_paused: site.is_paused,
                    status: site.status,
                    heartbeats: (site.heartbeats || []).slice(-24).map(hb => [hb.status, hb.ping])
                });
            }

            function renderMonitorsList() {
                const container = document.getElementById('monitorsList');
                if (!container) return;

                const existingIds = Array.from(container.children)
                    .map(el => el.id && el.id.replace('monitor-card-', ''))
                    .filter(Boolean)
                    .map(Number);
                const nextIds = monitors.map(s => s.id);
                const sameOrder = existingIds.length === nextIds.length &&
                    existingIds.every((id, i) => id === nextIds[i]);

                if (!sameOrder) {
                    // Structural change (added/removed/reordered) — full rebuild is unavoidable.
                    const preservedScrollTop = container.scrollTop;
                    container.innerHTML = monitors.map(buildMonitorCardHTML).join('');
                    container.scrollTop = preservedScrollTop;
                } else {
                    // Same set/order — only replace cards whose data actually changed,
                    // so unchanged cards keep their DOM node and don't retrigger CSS transitions.
                    monitors.forEach(site => {
                        const el = document.getElementById(`monitor-card-${site.id}`);
                        if (!el) return;
                        const sig = monitorCardSignature(site);
                        if (el.dataset.sig !== sig) {
                            const wrapper = document.createElement('div');
                            wrapper.innerHTML = buildMonitorCardHTML(site);
                            const newEl = wrapper.firstElementChild;
                            newEl.dataset.sig = sig;
                            el.replaceWith(newEl);
                        }
                    });
                }

                document.querySelectorAll('.monitor-item').forEach(el => {
                    if (!el.dataset.sig) {
                        const id = Number(el.id.replace('monitor-card-', ''));
                        const site = monitors.find(s => s.id === id);
                        if (site) el.dataset.sig = monitorCardSignature(site);
                    }
                });

                highlightActiveCard();
                filterMonitors();
            }

            function highlightActiveCard(scrollIntoView) {
                document.querySelectorAll('.monitor-item').forEach(el => {
                    el.classList.remove('border-[var(--color-primary)]', 'bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)]', 'shadow-[inset_0_0_0_1px_var(--color-primary)]');
                });
                const activeCard = document.getElementById(`monitor-card-${activeId}`);
                if (activeCard) {
                    activeCard.classList.add('border-[var(--color-primary)]', 'bg-[color-mix(in_srgb,var(--color-bg-surface)_60%,transparent)]', 'shadow-[inset_0_0_0_1px_var(--color-primary)]');
                    if (scrollIntoView) {
                        // Scroll only the sidebar list container, not any ancestor (main content/window) —
                        // scrollIntoView() walks up every scrollable ancestor, which drags the main scroll
                        // along whenever the card isn't already vertically centered in the viewport.
                        const listContainer = document.getElementById('monitorsList');
                        if (listContainer) {
                            const targetTop = activeCard.offsetTop - (listContainer.clientHeight / 2) + (activeCard.clientHeight / 2);
                            listContainer.scrollTo({ top: targetTop, behavior: 'smooth' });
                        }
                    }
                }
            }

            function onTypeChange() {
                const type = document.getElementById('formType').value;
                document.querySelectorAll('.dynamic-panel').forEach(el => el.classList.add('hidden'));

                if (['http', 'http-keyword', 'http-json-query', 'browser', 'websocket'].includes(type)) {
                    document.getElementById('panelUrl').classList.remove('hidden');
                    document.getElementById('panelHttpOptions').classList.remove('hidden');
                    if (type === 'http-keyword' || type === 'http-json-query') {
                        document.getElementById('panelKeyword').classList.remove('hidden');
                    }
                } else if (type === 'dns') {
                    document.getElementById('panelHostPort').classList.remove('hidden');
                    document.getElementById('panelDns').classList.remove('hidden');
                    document.getElementById('hostLabel').innerText = 'Domain / Hostname';
                    document.getElementById('formPort').value = 53;
                } else if (['postgres', 'mysql', 'mongodb', 'redis', 'mssql', 'oracle'].includes(type)) {
                    document.getElementById('panelHostPort').classList.remove('hidden');
                    document.getElementById('panelDb').classList.remove('hidden');
                    document.getElementById('hostLabel').innerText = 'Database Host / IP';
                    document.getElementById('formPort').value = type === 'postgres' ? 5432 : type === 'mysql' ? 3306 : type === 'redis' ? 6379 : 1433;
                } else if (['tcp', 'ping', 'smtp', 'mqtt', 'rabbitmq', 'kafka', 'gamedig', 'steam'].includes(type)) {
                    document.getElementById('panelHostPort').classList.remove('hidden');
                    document.getElementById('hostLabel').innerText = 'Hostname / IP';
                    document.getElementById('formPort').value = type === 'smtp' ? 25 : type === 'mqtt' ? 1883 : 80;
                } else {
                    document.getElementById('panelUrl').classList.remove('hidden');
                }
            }

            function themeColor(name) {
                return getComputedStyle(document.documentElement).getPropertyValue(name).trim();
            }

            function initChart() {
                const canvas = document.getElementById('responseChart');
                if (!canvas) return;
                const ctx = canvas.getContext('2d');
                const primary = themeColor('--color-primary');
                const border = themeColor('--color-border');
                const bgSurface = themeColor('--color-bg-surface');
                const bgMain = themeColor('--color-bg-main');
                const textSecondary = themeColor('--color-text-secondary');
                const textMuted = themeColor('--color-text-muted');

                const gradient = ctx.createLinearGradient(0, 0, 0, 240);
                gradient.addColorStop(0, `color-mix(in srgb, ${primary} 35%, transparent)`);
                gradient.addColorStop(1, `color-mix(in srgb, ${primary} 0%, transparent)`);

                chartInstance = new Chart(ctx, {
                    type: 'line',
                    data: {
                        labels: [],
                        datasets: [{
                            label: 'Avg Ping',
                            data: [],
                            borderColor: primary,
                            borderWidth: 2.5,
                            pointBackgroundColor: primary,
                            pointBorderColor: bgMain,
                            pointRadius: 4,
                            tension: 0.35,
                            fill: true,
                            backgroundColor: gradient
                        }]
                    },
                    options: {
                        responsive: true,
                        maintainAspectRatio: false,
                        plugins: {
                            legend: { display: false },
                            tooltip: {
                                backgroundColor: bgSurface,
                                titleColor: textSecondary,
                                bodyColor: primary,
                                borderColor: border,
                                borderWidth: 1,
                                padding: 10,
                                displayColors: false,
                                callbacks: {
                                    label: context => `Latency: ${context.raw} ms`
                                }
                            }
                        },
                        scales: {
                            x: { grid: { color: `color-mix(in srgb, ${border} 50%, transparent)` }, ticks: { color: textMuted, font: { family: 'JetBrains Mono', size: 10 } } },
                            y: { grid: { color: `color-mix(in srgb, ${border} 50%, transparent)` }, ticks: { color: textMuted, font: { family: 'JetBrains Mono', size: 10 }, callback: v => v + ' ms' } }
                        }
                    }
                });

                initUptimePieChart();
            }

            const centerTextPlugin = {
                id: 'centerText',
                afterDraw(chart) {
                    if (chart.config.type !== 'doughnut' || !chart.config._centerText) return;
                    const { ctx, chartArea: { left, right, top, bottom } } = chart;
                    const centerX = (left + right) / 2;
                    const centerY = (top + bottom) / 2;
                    const { value, label, valueColor, labelColor } = chart.config._centerText;

                    ctx.save();
                    ctx.textAlign = 'center';
                    ctx.textBaseline = 'middle';

                    ctx.font = '700 22px Inter, sans-serif';
                    ctx.fillStyle = valueColor;
                    ctx.fillText(value, centerX, centerY - 10);

                    ctx.font = '600 10px Inter, sans-serif';
                    ctx.fillStyle = labelColor;
                    ctx.fillText(label, centerX, centerY + 12);
                    ctx.restore();
                }
            };

            function initUptimePieChart() {
                const canvas = document.getElementById('uptimePieChart');
                if (!canvas) return;
                const ctx = canvas.getContext('2d');
                const primary = themeColor('--color-primary');
                const error = themeColor('--color-error');
                const bgSurface = themeColor('--color-bg-surface');
                const textSecondary = themeColor('--color-text-secondary');
                const textPrimary = themeColor('--color-text-primary');
                const border = themeColor('--color-border');

                uptimePieChartInstance = new Chart(ctx, {
                    type: 'doughnut',
                    data: {
                        labels: ['Uptime', 'Downtime'],
                        datasets: [{
                            data: [100, 0],
                            backgroundColor: [primary, error],
                            borderColor: border,
                            borderWidth: 2
                        }]
                    },
                    options: {
                        responsive: true,
                        maintainAspectRatio: false,
                        cutout: '85%',
                        plugins: {
                            legend: { display: false },
                            tooltip: {
                                backgroundColor: bgSurface,
                                titleColor: textSecondary,
                                bodyColor: primary,
                                borderColor: border,
                                borderWidth: 1,
                                padding: 10,
                                callbacks: {
                                    label: context => `${context.label}: ${context.raw.toFixed(2)}%`
                                }
                            }
                        }
                    },
                    plugins: [centerTextPlugin]
                });

                uptimePieChartInstance.config._centerText = {
                    value: '100%',
                    label: 'UPTIME',
                    valueColor: textPrimary,
                    labelColor: textSecondary
                };
                uptimePieChartInstance.update();
            }

            function updateUptimePieChart(m) {
                if (!uptimePieChartInstance) return;

                let upCount = 0;
                let downCount = 0;
                if (m.heartbeats && m.heartbeats.length > 0) {
                    m.heartbeats.forEach(hb => hb.status === 'up' ? upCount++ : downCount++);
                }

                let uptimePct;
                let downtimePct;
                if (upCount + downCount > 0) {
                    uptimePct = (upCount / (upCount + downCount)) * 100;
                    downtimePct = 100 - uptimePct;
                } else {
                    uptimePct = parseFloat(m.uptime_24h) || 100;
                    downtimePct = 100 - uptimePct;
                }

                uptimePieChartInstance.data.datasets[0].data = [uptimePct, downtimePct];
                uptimePieChartInstance.config._centerText.value = `${uptimePct.toFixed(1)}%`;
                uptimePieChartInstance.update('none');

                const upEl = document.getElementById('pieUptimeValue');
                const downEl = document.getElementById('pieDowntimeValue');
                if (upEl) upEl.innerText = `${uptimePct.toFixed(2)}%`;
                if (downEl) downEl.innerText = `${downtimePct.toFixed(2)}%`;
            }

            // ---- VPS Performance panel (type === 'vps') ----
            let vpsChartInstance = null;
            let vpsCpuChartInstance = null;
            let vpsRamChartInstance = null;
            let vpsStorageChartInstance = null;
            let vpsRequestToken = 0;

            // Small doughnut "gauge" shared by the CPU/RAM/Storage cards — same
            // cutout/centerText treatment as the Uptime vs Downtime pie chart, just
            // a single used/remaining slice pair instead of up/down.
            function initVpsGaugeChart(canvasId, color) {
                const canvas = document.getElementById(canvasId);
                if (!canvas) return null;
                const ctx = canvas.getContext('2d');
                const trackColor = themeColor('--color-border');
                const bgSurface = themeColor('--color-bg-surface');
                const textSecondary = themeColor('--color-text-secondary');
                const textPrimary = themeColor('--color-text-primary');

                const chart = new Chart(ctx, {
                    type: 'doughnut',
                    data: {
                        labels: ['Used', 'Free'],
                        datasets: [{
                            data: [0, 100],
                            backgroundColor: [themeColor(color), `color-mix(in srgb, ${trackColor} 60%, transparent)`],
                            borderWidth: 0
                        }]
                    },
                    options: {
                        responsive: true,
                        maintainAspectRatio: false,
                        cutout: '78%',
                        plugins: {
                            legend: { display: false },
                            tooltip: {
                                backgroundColor: bgSurface, titleColor: textSecondary, bodyColor: textPrimary,
                                borderColor: trackColor, borderWidth: 1, padding: 8,
                                callbacks: { label: context => `${context.label}: ${context.raw.toFixed(1)}%` }
                            }
                        }
                    },
                    plugins: [centerTextPlugin]
                });
                chart.config._centerText = { value: '0%', label: '', valueColor: textPrimary, labelColor: textSecondary };
                return chart;
            }

            function updateVpsGaugeChart(chart, pct, label) {
                if (!chart) return;
                const clamped = Math.max(0, Math.min(100, pct));
                chart.data.datasets[0].data = [clamped, 100 - clamped];
                chart.config._centerText.value = `${Math.round(clamped)}%`;
                chart.config._centerText.label = label;
                chart.update('none');
            }

            function initVpsChart() {
                const canvas = document.getElementById('vpsHistoryChart');
                if (!canvas || vpsChartInstance) return;
                const ctx = canvas.getContext('2d');
                const primary = themeColor('--color-primary');
                const secondary = themeColor('--color-secondary');
                const warning = themeColor('--color-warning');
                const border = themeColor('--color-border');
                const bgSurface = themeColor('--color-bg-surface');
                const textSecondary = themeColor('--color-text-secondary');
                const textMuted = themeColor('--color-text-muted');

                vpsChartInstance = new Chart(ctx, {
                    type: 'line',
                    data: {
                        labels: [],
                        datasets: [
                            { label: 'CPU %', data: [], borderColor: primary, backgroundColor: 'transparent', borderWidth: 2, pointRadius: 0, tension: 0.3 },
                            { label: 'RAM %', data: [], borderColor: secondary, backgroundColor: 'transparent', borderWidth: 2, pointRadius: 0, tension: 0.3 },
                            { label: 'Disk %', data: [], borderColor: warning, backgroundColor: 'transparent', borderWidth: 2, pointRadius: 0, tension: 0.3 }
                        ]
                    },
                    options: {
                        responsive: true,
                        maintainAspectRatio: false,
                        interaction: { mode: 'index', intersect: false },
                        plugins: {
                            legend: { display: true, labels: { color: textSecondary, font: { size: 10 } } },
                            tooltip: {
                                backgroundColor: bgSurface, titleColor: textSecondary, borderColor: border, borderWidth: 1, padding: 10,
                                callbacks: { label: context => `${context.dataset.label}: ${context.raw}%` }
                            }
                        },
                        scales: {
                            x: { grid: { color: `color-mix(in srgb, ${border} 50%, transparent)` }, ticks: { color: textMuted, font: { family: 'JetBrains Mono', size: 10 } } },
                            y: { min: 0, max: 100, grid: { color: `color-mix(in srgb, ${border} 50%, transparent)` }, ticks: { stepSize: 10, color: textMuted, font: { family: 'JetBrains Mono', size: 10 }, callback: v => v + '%' } }
                        }
                    }
                });
            }

            function formatMb(mb) {
                if (mb == null) return 'N/A';
                return mb >= 1024 ? `${(mb / 1024).toFixed(1)} GB` : `${Math.round(mb)} MB`;
            }

            function formatBytesRate(bytes) {
                if (bytes == null) return 'N/A';
                if (bytes >= 1024 * 1024 * 1024) return `${(bytes / 1024 / 1024 / 1024).toFixed(2)} GB`;
                if (bytes >= 1024 * 1024) return `${(bytes / 1024 / 1024).toFixed(1)} MB`;
                if (bytes >= 1024) return `${(bytes / 1024).toFixed(1)} KB`;
                return `${bytes} B`;
            }

            function formatUptime(seconds) {
                if (seconds == null) return 'N/A';
                const days = Math.floor(seconds / 86400);
                const hours = Math.floor((seconds % 86400) / 3600);
                if (days > 0) return `${days}d ${hours}h`;
                const mins = Math.floor((seconds % 3600) / 60);
                return `${hours}h ${mins}m`;
            }

            function pctBarColor(pct) {
                if (pct >= 90) return '--color-error';
                if (pct >= 75) return '--color-warning';
                return '--color-primary';
            }

            function fetchAndRenderVpsMetrics(m) {
                const token = ++vpsRequestToken;
                fetch(`/api/monitors/${m.id}/vps-metrics?range=1h`)
                    .then(r => r.json())
                    .then(res => {
                        if (token !== vpsRequestToken || activeId !== m.id) return; // superseded
                        const metrics = (res.status && res.data && res.data.metrics) ? res.data.metrics : [];
                        renderVpsPerformance(metrics);
                    })
                    .catch(() => { });
            }

            function renderVpsPerformance(metrics) {
                // No empty-state placeholder — with no metrics yet, `latest` is just an
                // empty object and every gauge/chart below renders at its zero state
                // instead of a "waiting for agent" message.
                const latest = metrics.length > 0 ? metrics[metrics.length - 1] : {};

                document.getElementById('vpsLastSeen').innerText = latest.timestamp
                    ? `Last report: ${new Date(latest.timestamp).toLocaleTimeString()}`
                    : 'Waiting for agent…';

                if (!vpsCpuChartInstance) vpsCpuChartInstance = initVpsGaugeChart('vpsCpuChart', '--color-primary');
                if (!vpsRamChartInstance) vpsRamChartInstance = initVpsGaugeChart('vpsRamChart', '--color-secondary');
                if (!vpsStorageChartInstance) vpsStorageChartInstance = initVpsGaugeChart('vpsStorageChart', '--color-warning');

                const cpuPct = latest.cpu_pct || 0;
                updateVpsGaugeChart(vpsCpuChartInstance, cpuPct, 'CPU');
                document.getElementById('vpsLoadText').innerText = `Load: ${(latest.load1 ?? 0).toFixed(2)}, ${(latest.load5 ?? 0).toFixed(2)}, ${(latest.load15 ?? 0).toFixed(2)}`;

                const ramTotal = latest.ram_total_mb || 0;
                const ramPct = ramTotal > 0 ? (latest.ram_used_mb / ramTotal) * 100 : 0;
                updateVpsGaugeChart(vpsRamChartInstance, ramPct, 'RAM');
                document.getElementById('vpsSwapText').innerText = `${formatMb(latest.ram_used_mb)} / ${formatMb(latest.ram_total_mb)}`
                    + (latest.swap_total_mb ? ` · Swap ${formatMb(latest.swap_used_mb)}/${formatMb(latest.swap_total_mb)}` : '');

                document.getElementById('vpsUptimeText').innerText = formatUptime(latest.uptime_seconds);
                document.getElementById('vpsNetText').innerText = `↓ ${formatBytesRate(latest.net_rx_bytes)}  ↑ ${formatBytesRate(latest.net_tx_bytes)}`;

                // Primary disk (root, or the first reported mount) drives the Storage
                // gauge card; any remaining mounts are listed below it.
                const disks = Array.isArray(latest.disks) ? latest.disks : [];
                const primaryDisk = disks.find(d => d.mount === '/') || disks[0];
                const storageLabel = document.getElementById('vpsStorageLabel');
                if (primaryDisk) {
                    const storagePct = primaryDisk.total_mb > 0 ? (primaryDisk.used_mb / primaryDisk.total_mb) * 100 : 0;
                    updateVpsGaugeChart(vpsStorageChartInstance, storagePct, 'DISK');
                    storageLabel.innerText = disks.length > 1 ? `Storage (${primaryDisk.mount})` : 'Storage';
                    document.getElementById('vpsStorageText').innerText = `${formatMb(primaryDisk.used_mb)} / ${formatMb(primaryDisk.total_mb)}`;
                } else {
                    updateVpsGaugeChart(vpsStorageChartInstance, 0, 'DISK');
                    storageLabel.innerText = 'Storage';
                    document.getElementById('vpsStorageText').innerText = 'No disk data reported.';
                }

                const otherDisks = primaryDisk ? disks.filter(d => d !== primaryDisk) : [];
                const disksExtra = document.getElementById('vpsDisksExtra');
                const disksContainer = document.getElementById('vpsDisksList');
                disksExtra.classList.toggle('hidden', otherDisks.length === 0);
                disksContainer.innerHTML = '';
                otherDisks.forEach(d => {
                    const pct = d.total_mb > 0 ? Math.round((d.used_mb / d.total_mb) * 100) : 0;
                    const row = document.createElement('div');
                    row.innerHTML = `
                        <div class="flex items-center justify-between mb-1">
                            <span class="text-xs font-mono text-[var(--color-text-secondary)]">${escapeHtml(d.mount)}</span>
                            <span class="text-xs font-mono text-[var(--color-text-primary)]">${formatMb(d.used_mb)} / ${formatMb(d.total_mb)} (${pct}%)</span>
                        </div>
                        <div class="h-2 rounded-full bg-[var(--color-bg-main)] border border-[var(--color-border)] overflow-hidden">
                            <div class="h-full rounded-full bg-[var(${pctBarColor(pct)})]" style="width:${Math.min(100, pct)}%"></div>
                        </div>`;
                    disksContainer.appendChild(row);
                });

                const hasNginx = latest.nginx_active_connections != null;
                document.getElementById('vpsNginxCards').classList.remove('hidden');
                document.getElementById('vpsNginxActive').innerText = hasNginx ? latest.nginx_active_connections : '-';
                document.getElementById('vpsNginxRequests').innerText = hasNginx ? (latest.nginx_requests_total ?? 'N/A') : '-';
                document.getElementById('vpsNginxErrorSize').innerText = hasNginx ? formatBytesRate(latest.nginx_error_log_size_bytes) : '-';
                document.getElementById('vpsNginxAccessSize').innerText = hasNginx ? formatBytesRate(latest.nginx_access_log_size_bytes) : '-';

                const errors = hasNginx && Array.isArray(latest.nginx_recent_errors) ? latest.nginx_recent_errors : [];
                document.getElementById('vpsNginxErrors').innerHTML = errors.length > 0
                    ? errors.map(line => `<div class="whitespace-pre">${escapeHtml(line)}</div>`).join('')
                    : '<div class="text-[var(--color-text-muted)]">No recent errors.</div>';

                const accessLines = hasNginx && Array.isArray(latest.nginx_recent_access) ? latest.nginx_recent_access : [];
                document.getElementById('vpsNginxAccess').innerHTML = accessLines.length > 0
                    ? accessLines.map(line => `<div class="whitespace-pre">${escapeHtml(line)}</div>`).join('')
                    : '<div class="text-[var(--color-text-muted)]">No recent access log lines.</div>';

                initVpsChart();
                if (vpsChartInstance) {
                    vpsChartInstance.data.labels = metrics.map(mt => formatLocalTime(mt.timestamp));
                    vpsChartInstance.data.datasets[0].data = metrics.map(mt => Math.round(mt.cpu_pct || 0));
                    vpsChartInstance.data.datasets[1].data = metrics.map(mt => mt.ram_total_mb > 0 ? Math.round((mt.ram_used_mb / mt.ram_total_mb) * 100) : 0);
                    vpsChartInstance.data.datasets[2].data = metrics.map(mt => {
                        const mtDisks = Array.isArray(mt.disks) ? mt.disks : [];
                        const mtPrimaryDisk = mtDisks.find(d => d.mount === '/') || mtDisks[0];
                        return mtPrimaryDisk && mtPrimaryDisk.total_mb > 0 ? Math.round((mtPrimaryDisk.used_mb / mtPrimaryDisk.total_mb) * 100) : 0;
                    });
                    vpsChartInstance.update('none');
                }
            }

            function showVpsInstallModal() {
                const m = monitors.find(x => x.id === activeId);
                if (!m) return;
                fetch(`/api/monitors/${m.id}/agent-install`)
                    .then(r => r.json())
                    .then(res => {
                        if (!res.status || !res.data || !res.data.docker_run) {
                            showToast(res.message || 'Failed to load install command', 'error');
                            return;
                        }

                        const tabs = [
                            { key: 'run', label: 'docker run', content: res.data.docker_run },
                            { key: 'compose', label: 'docker compose', content: res.data.docker_compose_install }
                        ];
                        let activeTab = 'run';

                        const overlay = document.createElement('div');
                        overlay.style.cssText = 'position:fixed;inset:0;z-index:10000;background:rgba(0,0,0,0.55);display:flex;align-items:center;justify-content:center;padding:1rem;';
                        const card = document.createElement('div');
                        card.style.cssText = 'width:100%;max-width:42rem;background:var(--color-bg-surface);border:1px solid color-mix(in srgb, var(--color-border) 80%, transparent);border-radius:1rem;box-shadow:0 25px 50px -12px rgba(0,0,0,0.5);padding:1.5rem;max-height:90vh;overflow:auto;';
                        card.innerHTML = `
                            <h3 style="font-size:1rem;font-weight:800;color:var(--color-text-primary);margin:0 0 0.75rem">Agent install command</h3>
                            ${res.data.warning ? `<p style="font-size:0.8125rem;line-height:1.5;margin:0 0 1rem;padding:0.75rem;border-radius:0.5rem;background:color-mix(in srgb, var(--color-error) 12%, transparent);border:1px solid color-mix(in srgb, var(--color-error) 30%, transparent);color:var(--color-error);font-weight:600">${res.data.warning}</p>` : ''}
                            <div data-tabs style="display:flex;gap:0.375rem;margin:0 0 0.75rem"></div>
                            <p data-tab-hint style="font-size:0.75rem;color:var(--color-text-muted);margin:0 0 0.5rem"></p>
                            <pre style="white-space:pre-wrap;word-break:break-all;background:var(--color-bg-main);border:1px solid color-mix(in srgb, var(--color-border) 70%, transparent);border-radius:0.5rem;padding:0.875rem;font-family:'JetBrains Mono',monospace;font-size:0.75rem;color:var(--color-text-primary);margin:0 0 1rem;max-height:16rem;overflow:auto"></pre>
                            <details style="margin:0 0 1rem">
                                <summary style="cursor:pointer;font-size:0.8125rem;font-weight:700;color:var(--color-text-secondary)">Nginx stub_status config (only needed if you want Nginx stats)</summary>
                                <p style="font-size:0.75rem;color:var(--color-text-muted);margin:0.5rem 0">Add this <code>server</code> block inside Nginx's <code>http {}</code> block, then reload Nginx. Don't publish this port in any compose <code>ports:</code> — leaving it unpublished keeps it reachable only from containers on the same Docker network as this agent.</p>
                                <pre style="white-space:pre-wrap;word-break:break-all;background:var(--color-bg-main);border:1px solid color-mix(in srgb, var(--color-border) 70%, transparent);border-radius:0.5rem;padding:0.875rem;font-family:'JetBrains Mono',monospace;font-size:0.75rem;color:var(--color-text-primary);margin:0;max-height:12rem;overflow:auto"></pre>
                            </details>
                            <div style="display:flex;justify-content:space-between;gap:0.5rem">
                                <button type="button" data-regen style="padding:0.5rem 1rem;border-radius:0.5rem;font-size:0.8125rem;font-weight:700;background:transparent;color:var(--color-error);border:1px solid color-mix(in srgb, var(--color-error) 40%, transparent);cursor:pointer">Regenerate Token</button>
                                <div style="display:flex;gap:0.5rem">
                                    <button type="button" data-copy style="padding:0.5rem 1rem;border-radius:0.5rem;font-size:0.8125rem;font-weight:700;background:transparent;color:var(--color-text-secondary);border:1px solid color-mix(in srgb, var(--color-border) 70%, transparent);cursor:pointer">Copy</button>
                                    <button type="button" data-close style="padding:0.5rem 1rem;border-radius:0.5rem;font-size:0.8125rem;font-weight:700;border:none;cursor:pointer;color:var(--color-bg-main);background:var(--color-primary)">Close</button>
                                </div>
                            </div>
                        `;

                        const tabsWrap = card.querySelector('[data-tabs]');
                        const pre = card.querySelector('pre');
                        const hint = card.querySelector('[data-tab-hint]');
                        const nginxPre = card.querySelectorAll('pre')[1];
                        nginxPre.textContent = res.data.nginx_stub_status_snippet || '';

                        function renderTab() {
                            const tab = tabs.find(t => t.key === activeTab);
                            pre.textContent = tab.content;
                            hint.textContent = activeTab === 'compose'
                                ? 'Writes docker-compose.yml alongside the agent source, then `docker compose up -d --build`. Stop it later with `docker compose down` from the same directory.'
                                : 'One-shot `docker run` — stop/remove it later with `docker stop uptinger-agent && docker rm uptinger-agent`.';
                            Array.from(tabsWrap.children).forEach(btn => {
                                const isActive = btn.dataset.key === activeTab;
                                btn.style.background = isActive ? 'var(--color-primary)' : 'transparent';
                                btn.style.color = isActive ? 'var(--color-bg-main)' : 'var(--color-text-secondary)';
                                btn.style.border = isActive ? 'none' : '1px solid color-mix(in srgb, var(--color-border) 70%, transparent)';
                            });
                        }
                        tabs.forEach(t => {
                            const btn = document.createElement('button');
                            btn.type = 'button';
                            btn.dataset.key = t.key;
                            btn.textContent = t.label;
                            btn.style.cssText = 'padding:0.375rem 0.75rem;border-radius:0.5rem;font-size:0.75rem;font-weight:700;cursor:pointer';
                            btn.addEventListener('click', () => { activeTab = t.key; renderTab(); });
                            tabsWrap.appendChild(btn);
                        });
                        renderTab();

                        overlay.appendChild(card);
                        document.body.appendChild(overlay);

                        const close = () => overlay.remove();
                        overlay.addEventListener('click', (e) => { if (e.target === overlay) close(); });
                        card.querySelector('[data-close]').addEventListener('click', close);
                        card.querySelector('[data-copy]').addEventListener('click', () => {
                            navigator.clipboard.writeText(tabs.find(t => t.key === activeTab).content).then(() => showToast('Copied to clipboard', 'success'));
                        });
                        card.querySelector('[data-regen]').addEventListener('click', async () => {
                            const ok = await openConfirmModal({
                                title: 'Regenerate agent token?',
                                message: 'The old token stops working immediately. You will need to reinstall the agent on the VPS with the new command.',
                                confirmLabel: 'Regenerate',
                                danger: true
                            });
                            if (!ok) return;
                            fetch(`/api/monitors/${m.id}/agent-token/regenerate`, { method: 'POST' })
                                .then(r => r.json())
                                .then(() => { close(); showVpsInstallModal(); });
                        });
                    })
                    .catch(() => showToast('Failed to load install command', 'error'));
            }

            function selectMonitor(id) {
                activeId = id;
                setMonitorIdInUrl(id);
                highlightActiveCard(true);
                renderActiveMonitor(id);
            }

            let lastActiveMonitorId = null;

            function renderActiveMonitor(id) {
                const m = monitors.find(x => x.id === id);
                if (!m) return;

                // renderActiveMonitor() is called on every 5s live-refresh tick, not just on an
                // actual selection change (see refreshDashboardData) — only reset mobile-analytics
                // state when the selection genuinely changed, otherwise this would wipe the feed
                // (and cause the logs table to flash "No mobile events yet") every tick, which was
                // itself a source of the jerk.
                const monitorChanged = id !== lastActiveMonitorId;
                lastActiveMonitorId = id;
                if (monitorChanged) {
                    mobileEventFeedCache = [];
                    mobileAnalyticsSignature = null;
                }

                const nameEl = document.getElementById('activeName');
                if (nameEl) nameEl.innerText = m.name;
                const idEl = document.getElementById('activeId');
                if (idEl) idEl.innerText = `#${m.id}`;
                const badgeEl = document.getElementById('activeTypeBadge');
                if (badgeEl) badgeEl.innerText = (m.type || 'http').toUpperCase();

                const urlEl = document.getElementById('activeUrl');
                if (urlEl) urlEl.href = m.url || '#';
                const urlTextEl = document.getElementById('activeUrlText');
                if (urlTextEl) urlTextEl.innerText = m.url || m.hostname || 'Configured Target';

                const minStr = Math.round((m.interval_seconds || 60) / 60);
                const intervalEl = document.getElementById('activeIntervalText');
                if (intervalEl) intervalEl.innerText = `Check every ${m.interval_seconds || 60} seconds (${minStr} minute${minStr > 1 ? 's' : ''})`;

                const bigBadge = document.getElementById('bigStatusBadge');
                const pauseBtn = document.getElementById('pauseBtnText');
                if (bigBadge && pauseBtn) {
                    if (m.is_paused) {
                        bigBadge.innerText = 'Paused';
                        bigBadge.className = 'px-8 py-3 rounded-full bg-[var(--color-warning)] text-[var(--color-bg-main)] font-black text-2xl tracking-wide shadow-lg shadow-[color-mix(in_srgb,var(--color-warning)_30%,transparent)] flex items-center justify-center';
                        pauseBtn.innerText = 'Resume';
                    } else if (m.status === 'ONLINE') {
                        bigBadge.innerText = 'Up';
                        bigBadge.className = 'px-8 py-3 rounded-full bg-[var(--color-primary)] text-[var(--color-bg-main)] font-black text-2xl tracking-wide shadow-lg shadow-[color-mix(in_srgb,var(--color-primary)_30%,transparent)] flex items-center justify-center';
                        pauseBtn.innerText = 'Pause';
                    } else {
                        bigBadge.innerText = 'Down';
                        bigBadge.className = 'px-8 py-3 rounded-full bg-[var(--color-error)] text-[var(--color-text-primary)] font-black text-2xl tracking-wide shadow-lg shadow-[color-mix(in_srgb,var(--color-error)_30%,transparent)] flex items-center justify-center';
                        pauseBtn.innerText = 'Pause';
                    }
                }

                if (document.getElementById('metricCurrent')) document.getElementById('metricCurrent').innerText = m.current_response || 'N/A';
                if (document.getElementById('metricAvg24h')) document.getElementById('metricAvg24h').innerText = m.avg_response_24h || 'N/A';
                if (document.getElementById('metricUptime24h')) document.getElementById('metricUptime24h').innerText = m.uptime_24h || '100%';
                if (document.getElementById('metricUptime30d')) document.getElementById('metricUptime30d').innerText = m.uptime_30d || '100%';
                if (document.getElementById('metricUptime1y')) document.getElementById('metricUptime1y').innerText = m.uptime_1y || '100%';

                if (['http', 'browser'].includes(m.type)) {
                    if (document.getElementById('metricCertSub')) document.getElementById('metricCertSub').innerText = `( ${m.cert_exp_date || 'N/A'} )`;
                    if (document.getElementById('metricCertDays')) document.getElementById('metricCertDays').innerText = m.cert_exp_days || 'N/A';
                    if (document.getElementById('metricDomainSub')) document.getElementById('metricDomainSub').innerText = `( ${m.domain_exp_date || 'N/A'} )`;
                    if (document.getElementById('metricDomainDays')) document.getElementById('metricDomainDays').innerText = m.domain_exp_days || 'N/A';
                } else {
                    if (document.getElementById('metricCertSub')) document.getElementById('metricCertSub').innerText = '( Non-SSL )';
                    if (document.getElementById('metricCertDays')) document.getElementById('metricCertDays').innerText = 'N/A';
                    if (document.getElementById('metricDomainSub')) document.getElementById('metricDomainSub').innerText = '( N/A )';
                    if (document.getElementById('metricDomainDays')) document.getElementById('metricDomainDays').innerText = 'N/A';
                }

                const hbBarContainer = document.getElementById('activeHeartbeatBar');
                if (hbBarContainer) {
                    hbBarContainer.innerHTML = '';

                    const hbCellCount = 48;
                    const hbList = (m.heartbeats || []).slice(-hbCellCount);
                    const hbEmptyCount = Math.max(0, hbCellCount - hbList.length);

                    for (let i = 0; i < hbEmptyCount; i++) {
                        const cell = document.createElement('span');
                        cell.className = 'h-9 flex-1 min-w-0 rounded-md border border-[var(--color-border)] opacity-40';
                        hbBarContainer.appendChild(cell);
                    }

                    hbList.forEach(hb => {
                        const span = document.createElement('span');
                        span.className = `h-9 flex-1 min-w-0 rounded-md ${hb.status === 'up' ? 'bg-[var(--color-primary)]' : 'bg-[var(--color-error)]'} hover:opacity-80 transition-all cursor-pointer`;
                        span.title = `Ping: ${hb.ping}ms (${formatLocalTime(hb.timestamp) || hb.time})`;
                        hbBarContainer.appendChild(span);
                    });

                    const pingIndicator = document.createElement('span');
                    pingIndicator.className = 'h-9 flex-1 min-w-0 rounded-md bg-[var(--color-secondary)] shadow-[0_0_8px_color-mix(in_srgb,var(--color-secondary)_80%,transparent)] animate-pulse';
                    pingIndicator.title = 'Checking...';
                    hbBarContainer.appendChild(pingIndicator);
                }

                renderLogsTable();

                const isVps = m.type === 'vps';
                const isMobile = m.type === 'mobile';
                const vpsCard = document.getElementById('vpsPerformanceCard');
                const mobileCard = document.getElementById('mobileAnalyticsCard');
                const httpCard = document.getElementById('httpMetricsCard');
                const chartRow = document.getElementById('responseChartRow');
                if (vpsCard) vpsCard.classList.toggle('hidden', !isVps);
                if (mobileCard) mobileCard.classList.toggle('hidden', !isMobile);
                if (httpCard) httpCard.classList.toggle('hidden', isVps || isMobile);
                if (chartRow) chartRow.classList.toggle('hidden', isVps || isMobile);

                if (isMobile) {
                    if (monitorChanged) startMobileAnalyticsPolling(m);
                } else {
                    stopMobileAnalyticsPolling();
                    if (isVps) {
                        fetchAndRenderVpsMetrics(m);
                    } else {
                        updateResponseChart(m);
                        updateUptimePieChart(m);
                    }
                }
            }

            // Mobile analytics are batch-derived, not real-time like heartbeats — polled on its
            // own slower interval (not the 5s live-refresh tick, which calls renderActiveMonitor()
            // far more often than this data actually changes) so a mobile monitor's DAU/crash/
            // event-feed cards don't flicker every 5 seconds. Started on selecting a mobile
            // monitor, stopped on navigating away from one.
            const MOBILE_ANALYTICS_POLL_MS = 20000;
            let mobileAnalyticsTimer = null;
            let mobileAnalyticsSignature = null;
            let mobileDauChartInstance = null;
            let mobileCrashPieChartInstance = null;

            function initMobileDauChart() {
                const canvas = document.getElementById('mobileDauChart');
                if (!canvas) return;
                const ctx = canvas.getContext('2d');
                const primary = themeColor('--color-primary');
                const border = themeColor('--color-border');
                const bgSurface = themeColor('--color-bg-surface');
                const bgMain = themeColor('--color-bg-main');
                const textSecondary = themeColor('--color-text-secondary');
                const textMuted = themeColor('--color-text-muted');

                const gradient = ctx.createLinearGradient(0, 0, 0, 220);
                gradient.addColorStop(0, `color-mix(in srgb, ${primary} 35%, transparent)`);
                gradient.addColorStop(1, `color-mix(in srgb, ${primary} 0%, transparent)`);

                mobileDauChartInstance = new Chart(ctx, {
                    type: 'line',
                    data: {
                        labels: [],
                        datasets: [{
                            label: 'Active Users',
                            data: [],
                            borderColor: primary,
                            borderWidth: 2.5,
                            pointBackgroundColor: primary,
                            pointBorderColor: bgMain,
                            pointRadius: 3,
                            tension: 0.35,
                            fill: true,
                            backgroundColor: gradient
                        }]
                    },
                    options: {
                        responsive: true,
                        maintainAspectRatio: false,
                        plugins: {
                            legend: { display: false },
                            tooltip: {
                                backgroundColor: bgSurface,
                                titleColor: textSecondary,
                                bodyColor: primary,
                                borderColor: border,
                                borderWidth: 1,
                                padding: 10,
                                displayColors: false,
                                callbacks: {
                                    label: context => `${context.raw} active users`
                                }
                            }
                        },
                        scales: {
                            x: { grid: { color: `color-mix(in srgb, ${border} 50%, transparent)` }, ticks: { color: textMuted, font: { family: 'JetBrains Mono', size: 10 } } },
                            y: { beginAtZero: true, grid: { color: `color-mix(in srgb, ${border} 50%, transparent)` }, ticks: { color: textMuted, font: { family: 'JetBrains Mono', size: 10 }, precision: 0 } }
                        }
                    }
                });
            }

            function updateMobileDauChart(series) {
                if (!mobileDauChartInstance) {
                    initMobileDauChart();
                    if (!mobileDauChartInstance) return;
                }
                const rows = series || [];
                mobileDauChartInstance.data.labels = rows.map(r => {
                    const d = new Date(`${r.date}T00:00:00Z`);
                    return d.toLocaleDateString(undefined, { month: 'short', day: 'numeric' });
                });
                mobileDauChartInstance.data.datasets[0].data = rows.map(r => r.count);
                mobileDauChartInstance.update('none');
            }

            function initMobileCrashPieChart() {
                const canvas = document.getElementById('mobileCrashPieChart');
                if (!canvas) return;
                const ctx = canvas.getContext('2d');
                const primary = themeColor('--color-primary');
                const error = themeColor('--color-error');
                const bgSurface = themeColor('--color-bg-surface');
                const textSecondary = themeColor('--color-text-secondary');
                const textPrimary = themeColor('--color-text-primary');
                const border = themeColor('--color-border');

                mobileCrashPieChartInstance = new Chart(ctx, {
                    type: 'doughnut',
                    data: {
                        labels: ['Crash-Free', 'Crashed'],
                        datasets: [{
                            data: [100, 0],
                            backgroundColor: [primary, error],
                            borderColor: border,
                            borderWidth: 2
                        }]
                    },
                    options: {
                        responsive: true,
                        maintainAspectRatio: false,
                        cutout: '85%',
                        plugins: {
                            legend: { display: false },
                            tooltip: {
                                backgroundColor: bgSurface,
                                titleColor: textSecondary,
                                bodyColor: primary,
                                borderColor: border,
                                borderWidth: 1,
                                padding: 10,
                                callbacks: {
                                    label: context => `${context.label}: ${context.raw.toFixed(2)}%`
                                }
                            }
                        }
                    },
                    plugins: [centerTextPlugin]
                });

                mobileCrashPieChartInstance.config._centerText = {
                    value: '100%',
                    label: 'CRASH-FREE',
                    valueColor: textPrimary,
                    labelColor: textSecondary
                };
                mobileCrashPieChartInstance.update();
            }

            function updateMobileCrashPieChart(crashFreeRate) {
                if (!mobileCrashPieChartInstance) {
                    initMobileCrashPieChart();
                    if (!mobileCrashPieChartInstance) return;
                }
                const rate = crashFreeRate ? crashFreeRate.rate : 1;
                const crashFreePct = Math.max(0, Math.min(1, rate)) * 100;
                const crashedPct = 100 - crashFreePct;

                mobileCrashPieChartInstance.data.datasets[0].data = [crashFreePct, crashedPct];
                mobileCrashPieChartInstance.config._centerText.value = `${crashFreePct.toFixed(1)}%`;
                mobileCrashPieChartInstance.update('none');

                const freeEl = document.getElementById('pieCrashFreeValue');
                const crashedEl = document.getElementById('pieCrashedValue');
                if (freeEl) freeEl.innerText = `${crashFreePct.toFixed(2)}%`;
                if (crashedEl) crashedEl.innerText = `${crashedPct.toFixed(2)}%`;
            }

            function startMobileAnalyticsPolling(m) {
                stopMobileAnalyticsPolling();
                fetchAndRenderMobileAnalytics(m);
                mobileAnalyticsTimer = setInterval(() => fetchAndRenderMobileAnalytics(m), MOBILE_ANALYTICS_POLL_MS);
            }

            function stopMobileAnalyticsPolling() {
                if (mobileAnalyticsTimer) {
                    clearInterval(mobileAnalyticsTimer);
                    mobileAnalyticsTimer = null;
                }
            }

            async function fetchAndRenderMobileAnalytics(m) {
                try {
                    const dauRange = document.getElementById('mobileDauRangeFilter')?.value || '7d';
                    const res = await fetch(`/api/monitors/${m.id}/mobile-analytics?range=${dauRange}`);
                    const json = await res.json();
                    if (!json.status || !json.data) return;
                    const d = json.data;

                    // Skip the DOM rewrite entirely when nothing actually changed since the last
                    // poll — the 20s interval alone doesn't guarantee new data arrived in that
                    // window, and re-writing identical innerHTML still flashes hover/tooltip state.
                    const signature = JSON.stringify({
                        dau: d.dau, wau: d.wau, mau: d.mau,
                        dau_series: (d.dau_series || []).map(r => `${r.date}:${r.count}`),
                        sessions: d.crash_free_rate && d.crash_free_rate.sessions,
                        rate: d.crash_free_rate && d.crash_free_rate.rate,
                        issues: (d.crash_issues || []).map(i => `${i.signature}:${i.occurrence_count}`),
                        events: (d.custom_events || []).map(e => `${e.event_name}:${e.count}`),
                        versions: (d.version_breakdown || []).map(v => `${v.app_version}:${v.count}`),
                        os: (d.os_breakdown || []).map(o => `${o.os_name} ${o.os_version}:${o.count}`),
                        region: (d.region_breakdown || []).map(r => `${r.region}:${r.count}`),
                        feed: (d.event_feed || []).map(ev => ev.id)
                    });
                    if (signature === mobileAnalyticsSignature) return;
                    mobileAnalyticsSignature = signature;

                    document.getElementById('mobileDau').textContent = d.dau ?? '-';
                    document.getElementById('mobileWau').textContent = d.wau ?? '-';
                    document.getElementById('mobileMau').textContent = d.mau ?? '-';
                    updateMobileDauChart(d.dau_series);
                    updateMobileCrashPieChart(d.crash_free_rate);
                    document.getElementById('mobileSessions').textContent = (d.crash_free_rate && d.crash_free_rate.sessions) ?? 0;
                    const rate = d.crash_free_rate ? Math.round(d.crash_free_rate.rate * 1000) / 10 : 100;
                    document.getElementById('mobileCrashFreeRate').textContent = `${rate}%`;

                    const issuesEl = document.getElementById('mobileCrashIssues');
                    const issues = d.crash_issues || [];
                    issuesEl.innerHTML = issues.length
                        ? issues.map(i => `<div class="flex items-center justify-between gap-2 py-1 border-b border-[var(--color-border)] last:border-0">
                                <span class="truncate text-[var(--color-text-primary)]" title="${escapeHtml(i.title || i.signature)}">${escapeHtml(i.title || i.signature)}</span>
                                <span class="shrink-0 text-[var(--color-error)] font-bold">${i.occurrence_count}x</span>
                            </div>`).join('')
                        : '<p class="text-[var(--color-text-muted)]">No crashes reported yet.</p>';

                    const eventsEl = document.getElementById('mobileCustomEvents');
                    const events = d.custom_events || [];
                    eventsEl.innerHTML = events.length
                        ? events.map(e => `<div class="flex items-center justify-between gap-2 py-1 border-b border-[var(--color-border)] last:border-0">
                                <span class="truncate text-[var(--color-text-primary)]">${escapeHtml(e.event_name || 'unnamed')}</span>
                                <span class="shrink-0">${e.count}</span>
                            </div>`).join('')
                        : '<p class="text-[var(--color-text-muted)]">No custom events yet.</p>';

                    const versionEl = document.getElementById('mobileVersionBreakdown');
                    const versions = d.version_breakdown || [];
                    versionEl.innerHTML = versions.length
                        ? versions.map(v => `<div class="flex items-center justify-between gap-2 py-1 border-b border-[var(--color-border)] last:border-0">
                                <span class="text-[var(--color-text-primary)]">${escapeHtml(v.app_version || 'unknown')}</span>
                                <span>${v.count} devices</span>
                            </div>`).join('')
                        : '<p class="text-[var(--color-text-muted)]">No data yet.</p>';

                    const osEl = document.getElementById('mobileOsBreakdown');
                    const osRows = d.os_breakdown || [];
                    osEl.innerHTML = osRows.length
                        ? osRows.map(o => `<div class="flex items-center justify-between gap-2 py-1 border-b border-[var(--color-border)] last:border-0">
                                <span class="text-[var(--color-text-primary)]">${escapeHtml(o.os_name || 'unknown')} ${escapeHtml(o.os_version || '')}</span>
                                <span>${o.count} devices</span>
                            </div>`).join('')
                        : '<p class="text-[var(--color-text-muted)]">No data yet.</p>';

                    const regionEl = document.getElementById('mobileRegionBreakdown');
                    const regionRows = d.region_breakdown || [];
                    regionEl.innerHTML = regionRows.length
                        ? regionRows.map(r => `<div class="flex items-center justify-between gap-2 py-1 border-b border-[var(--color-border)] last:border-0">
                                <span class="text-[var(--color-text-primary)]">${escapeHtml(r.region || 'unknown')}</span>
                                <span>${r.count} devices</span>
                            </div>`).join('')
                        : '<p class="text-[var(--color-text-muted)]">No data yet.</p>';

                    // Per-event detail (screen views, crashes, sessions) is rendered in the shared
                    // Heartbeat Event Logs table below rather than a separate list here — see
                    // renderLogsTable()'s mobile branch.
                    mobileEventFeedCache = d.event_feed || [];
                    renderLogsTable();
                } catch (e) {
                    console.error('Failed to load mobile analytics', e);
                }
            }

            function escapeHtml(str) {
                return String(str).replace(/[&<>"']/g, c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]));
            }

            function showMobileInstallModal() {
                const m = monitors.find(x => x.id === activeId);
                if (!m) return;
                fetch(`/api/monitors/${m.id}/mobile-install`)
                    .then(r => r.json())
                    .then(res => {
                        if (!res.status || !res.data) {
                            showToast(res.message || 'Failed to load mobile install info', 'error');
                            return;
                        }
                        const { ingest_url, token } = res.data;

                        document.getElementById('mobileInstallUrl').textContent = ingest_url;
                        document.getElementById('mobileInstallToken').textContent = token;

                        document.getElementById('mobileInstallCurl').textContent =
`curl -X POST "${ingest_url}" \\
  -H "Authorization: Bearer ${token}" \\
  -H "Content-Type: application/json" \\
  -d '{
    "device_id": "test-device-1",
    "session_id": "test-session-1",
    "app_version": "1.0.0",
    "os_name": "android",
    "os_version": "14",
    "events": [
      { "type": "session_start" },
      { "type": "custom", "name": "screen_view", "props": { "screen": "Home" } }
    ]
  }'`;

                        document.getElementById('mobileInstallFetch').textContent =
`async function sendUptingerEvents(events) {
  await fetch("${ingest_url}", {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      "Authorization": "Bearer ${token}"
    },
    body: JSON.stringify({
      device_id: getOrCreateDeviceId(), // your own stable per-install id
      app_version: "1.0.0",
      events
    })
  });
}

// e.g. on a screen change:
sendUptingerEvents([{ type: 'custom', name: 'screen_view', props: { screen: 'Home' } }]);`;

                        const modal = document.getElementById('mobileInstallModal');
                        const container = document.getElementById('mobileInstallContainer');
                        modal.classList.remove('hidden');
                        setTimeout(() => container.classList.remove('scale-95', 'opacity-0'), 10);
                    })
                    .catch(() => showToast('Failed to load mobile install info', 'error'));
            }

            function closeMobileInstallModal() {
                const modal = document.getElementById('mobileInstallModal');
                const container = document.getElementById('mobileInstallContainer');
                container.classList.add('scale-95', 'opacity-0');
                setTimeout(() => modal.classList.add('hidden'), 200);
            }

            // The server sends `time` pre-formatted in UTC ("HH:MM"), which doesn't match the
            // Logs table's display (built client-side via `new Date(...).toLocaleString()`, i.e.
            // the browser's local timezone). Recompute from the raw UTC `timestamp` here so chart
            // labels / tooltips line up with what the Logs table shows.
            function formatLocalTime(timestamp) {
                if (!timestamp) return null;
                const d = new Date(timestamp);
                if (Number.isNaN(d.getTime())) return null;
                return d.toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' });
            }

            // Not cached: the dashboard live-polls every 5s (LIVE_REFRESH_INTERVAL_MS), so a
            // cached response would go stale almost immediately and mask incoming checks.
            async function getChartHeartbeats(m, range) {
                if (range === 'Recent') return m.heartbeats || [];

                try {
                    const res = await fetch(`/api/monitors/${m.id}/heartbeats?range=${range}`);
                    const json = await res.json();
                    return (json.status && json.data && json.data.heartbeats) ? json.data.heartbeats : [];
                } catch {
                    return m.heartbeats || [];
                }
            }

            // Guards against a slow fetch resolving after the user has already switched monitor
            // or range, which would otherwise paint stale data over the current selection.
            let chartRequestToken = 0;

            async function updateResponseChart(m) {
                if (!chartInstance || !m || !m.heartbeats || m.heartbeats.length === 0) return;

                const filterEl = document.getElementById('chartTimeFilter');
                const range = filterEl ? filterEl.value : 'Recent';

                const token = ++chartRequestToken;
                const filtered = await getChartHeartbeats(m, range);
                if (token !== chartRequestToken || activeId !== m.id) return; // superseded by a newer request

                const lastHb = m.heartbeats[m.heartbeats.length - 1];
                const isDown = lastHb ? lastHb.status !== 'up' : (m.status !== 'ONLINE');
                const lineColorVar = isDown ? '--color-error' : '--color-primary';
                const lineColor = themeColor(lineColorVar);
                const ctx = chartInstance.ctx;
                const gradient = ctx.createLinearGradient(0, 0, 0, 240);
                gradient.addColorStop(0, `color-mix(in srgb, ${lineColor} 35%, transparent)`);
                gradient.addColorStop(1, `color-mix(in srgb, ${lineColor} 0%, transparent)`);

                chartInstance.data.labels = filtered.map(h => formatLocalTime(h.timestamp) || h.time);
                chartInstance.data.datasets[0].data = filtered.map(h => h.ping);
                chartInstance.data.datasets[0].borderColor = lineColor;
                chartInstance.data.datasets[0].pointBackgroundColor = lineColor;
                chartInstance.data.datasets[0].pointHoverBackgroundColor = lineColor;
                chartInstance.data.datasets[0].backgroundColor = gradient;
                chartInstance.update();

                const legendDot = document.getElementById('chartLegendDot');
                if (legendDot) {
                    legendDot.className = `w-3 h-3 rounded bg-[var(${lineColorVar})] inline-block shadow-[0_0_8px_color-mix(in_srgb,var(${lineColorVar})_80%,transparent)]`;
                }
            }

            function escapeHtml(str) {
                return String(str).replace(/[&<>"']/g, c => ({
                    '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;'
                }[c]));
            }

            function formatResponseHeaders(raw, logId) {
                if (!raw) return '<span class="text-[var(--color-text-muted)]">—</span>';
                let obj;
                try { obj = JSON.parse(raw); } catch { return '<span class="text-[var(--color-text-muted)]">—</span>'; }
                const summary = Object.entries(obj).map(([k, v]) => `${k}: ${v}`).join('; ');
                return `
                    <div class="flex items-center gap-1.5">
                        <span class="cursor-help truncate" title="${escapeHtml(summary)}">${escapeHtml(summary)}</span>
                        <button type="button" onclick="copyResponseHeaders(${logId}, this)" title="Copy response"
                            class="shrink-0 p-1 rounded-md hover:bg-[var(--color-bg-surface)] text-[var(--color-text-muted)] hover:text-[var(--color-text-primary)] transition-colors">
                            <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z" />
                            </svg>
                        </button>
                    </div>
                `;
            }

            // Shared by copyResponseHeaders and copyMobileEventJson — swaps the trigger button's
            // icon to a checkmark briefly on success, same pattern either caller used before this
            // was factored out.
            function copyTextToClipboard(text, btn, successMsg, failMsg) {
                const done = () => {
                    showToast(successMsg, 'success');
                    if (btn) {
                        const original = btn.innerHTML;
                        btn.innerHTML = `<svg class="w-3.5 h-3.5 text-[var(--color-success)]" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" /></svg>`;
                        setTimeout(() => { btn.innerHTML = original; }, 1200);
                    }
                };

                if (navigator.clipboard && navigator.clipboard.writeText) {
                    navigator.clipboard.writeText(text).then(done).catch(() => {
                        showToast(failMsg, 'error');
                    });
                } else {
                    const ta = document.createElement('textarea');
                    ta.value = text;
                    ta.style.position = 'fixed';
                    ta.style.opacity = '0';
                    document.body.appendChild(ta);
                    ta.select();
                    try { document.execCommand('copy'); done(); } catch { showToast(failMsg, 'error'); }
                    document.body.removeChild(ta);
                }
            }

            function copyResponseHeaders(logId, btn) {
                const m = monitors.find(x => x.id === activeId);
                const log = m && m.logs ? m.logs.find(l => l.id === logId) : null;
                if (!log || !log.response_headers) return;

                let text = log.response_headers;
                try { text = JSON.stringify(JSON.parse(log.response_headers), null, 2); } catch { /* copy raw as-is */ }

                copyTextToClipboard(text, btn, 'Response copied to clipboard', 'Failed to copy response');
            }

            // kind: 'device' copies the device-info JSON shown in the Device column; 'details'
            // copies whatever the Details column is showing for that row (stack trace, props, or
            // just the session id) — looked up by event id since mobileEventFeedCache is filtered/
            // re-ordered independently of what's currently rendered.
            function copyMobileEventJson(kind, eventId, btn) {
                const ev = (mobileEventFeedCache || []).find(e => e.id === eventId);
                if (!ev) return;

                let text;
                if (kind === 'device') {
                    text = JSON.stringify(mobileDevicePayload(ev), null, 2);
                } else if (ev.event_type === 'crash' || ev.event_type === 'error') {
                    text = ev.stack_trace ? `${ev.event_name || ''}\n\n${ev.stack_trace}` : (ev.event_name || '');
                } else if (ev.props && Object.keys(ev.props).length > 0) {
                    text = JSON.stringify(ev.props, null, 2);
                } else {
                    text = ev.session_id || '';
                }
                if (!text) return;

                copyTextToClipboard(text, btn, 'Copied to clipboard', 'Failed to copy');
            }

            function getFilteredLogs() {
                const m = monitors.find(x => x.id === activeId);
                const limitEl = document.getElementById('logsLimitFilter');
                const limit = limitEl ? parseInt(limitEl.value, 10) : 20;
                const statusEl = document.getElementById('logsStatusFilter');
                const statusFilter = statusEl ? statusEl.value : 'ALL';
                const dateEl = document.getElementById('logsDateFilter');
                const dateFilter = dateEl ? dateEl.value : '';

                let logs = (m && m.logs ? m.logs : []);
                if (statusFilter !== 'ALL') {
                    logs = logs.filter(log => log.status === statusFilter);
                }
                if (dateFilter) {
                    logs = logs.filter(log => log.timestamp && log.timestamp.slice(0, 10) === dateFilter);
                }
                return logs.slice(0, limit);
            }

            // The Status filter's meaning depends on the active monitor: Up/Down for heartbeat
            // checks, or event type (crash/error/session/custom) for mobile monitors. Swaps the
            // <option> list in place so the same <select id="logsStatusFilter"> serves both, and
            // resets to "ALL" whenever the kind changes (a stale "ERROR" selection carried over
            // to a mobile monitor would silently filter everything out).
            let logsStatusFilterKind = null;
            function updateLogsStatusFilterOptions(isMobile) {
                const kind = isMobile ? 'mobile' : 'heartbeat';
                if (logsStatusFilterKind === kind) return;
                logsStatusFilterKind = kind;
                const statusEl = document.getElementById('logsStatusFilter');
                if (!statusEl) return;
                statusEl.innerHTML = isMobile
                    ? `<option value="ALL" selected>All event types</option>
                       <option value="screen_view">Screen views</option>
                       <option value="custom">Custom (other)</option>
                       <option value="session_start">Session start</option>
                       <option value="session_end">Session end</option>
                       <option value="error">Error</option>
                       <option value="crash">Crash</option>`
                    : `<option value="ALL" selected>All statuses</option>
                       <option value="200 OK">Up (200 OK)</option>
                       <option value="ERROR">Down (Error)</option>`;
            }

            // "screen_view" is stored as event_type 'custom' with event_name 'screen_view' (see
            // mobile-monitoring/sdk.ts's logEvent calls, or the client's own SDK equivalent) —
            // the filter above treats it as its own kind since it dominates the feed, so match
            // on name here, not just type.
            function matchesEventTypeFilter(ev, filter) {
                if (filter === 'ALL') return true;
                if (filter === 'screen_view') return ev.event_type === 'custom' && ev.event_name === 'screen_view';
                if (filter === 'custom') return ev.event_type === 'custom' && ev.event_name !== 'screen_view';
                return ev.event_type === filter;
            }

            function mobileDevicePayload(ev) {
                const device = {
                    device_id: ev.device_id,
                    device_model: ev.device_model,
                    os: [ev.os_name, ev.os_version].filter(Boolean).join(' '),
                    app_version: ev.app_version,
                    build: ev.build_number,
                    region: ev.region,
                    locale: ev.locale,
                    timezone: ev.timezone
                };
                Object.keys(device).forEach(k => { if (!device[k]) delete device[k]; });
                return device;
            }

            const COPY_ICON_SVG = `<svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z" /></svg>`;
            function copyIconButton(kind, eventId) {
                return `<button type="button" onclick="copyMobileEventJson('${kind}', ${eventId}, this)" title="Copy"
                    class="shrink-0 p-1 rounded-md hover:bg-[var(--color-bg-surface)] text-[var(--color-text-muted)] hover:text-[var(--color-text-primary)] transition-colors">${COPY_ICON_SVG}</button>`;
            }

            function formatMobileDevice(ev) {
                const json = JSON.stringify(mobileDevicePayload(ev));
                return `<div class="flex items-center gap-1.5">
                        <span class="cursor-help truncate block max-w-[220px]" title="${escapeHtml(JSON.stringify(mobileDevicePayload(ev), null, 2))}">${escapeHtml(json)}</span>
                        ${copyIconButton('device', ev.id)}
                    </div>`;
            }

            function formatMobileEventDetails(ev) {
                if (ev.event_type === 'crash' || ev.event_type === 'error') {
                    // The actual error message (event_name) is the useful summary here — the Event
                    // badge next to it just says "crash"/"error" (see renderLogsTable), so this is
                    // where that message actually shows. Full message + stack trace both go in the
                    // tooltip and the copy button, even though only the message is visible inline.
                    const summary = ev.event_name || (ev.stack_trace || '').split('\n')[0] || '';
                    const full = ev.stack_trace ? `${ev.event_name || ''}\n\n${ev.stack_trace}` : (ev.event_name || '');
                    return `<div class="flex items-center gap-1.5">
                            <span class="cursor-help truncate block max-w-xs text-[var(--color-error)]" title="${escapeHtml(full)}">${escapeHtml(summary)}</span>
                            ${copyIconButton('details', ev.id)}
                        </div>`;
                }
                if (ev.props && Object.keys(ev.props).length > 0) {
                    const json = JSON.stringify(ev.props);
                    return `<div class="flex items-center gap-1.5">
                            <span class="cursor-help truncate block max-w-xs" title="${escapeHtml(JSON.stringify(ev.props, null, 2))}">${escapeHtml(json)}</span>
                            ${copyIconButton('details', ev.id)}
                        </div>`;
                }
                if (ev.session_id) {
                    return `<div class="flex items-center gap-1.5">
                            <span class="truncate block max-w-xs text-[var(--color-text-muted)]">session ${escapeHtml(ev.session_id.slice(0, 8))}</span>
                            ${copyIconButton('details', ev.id)}
                        </div>`;
                }
                return '<span class="text-[var(--color-text-muted)]">—</span>';
            }

            // Crash/error rows are the ones worth flagging red; everything else (screen views,
            // sessions, other custom events) shares one neutral blue so the red genuinely stands
            // out in the feed instead of competing with several other colors.
            const MOBILE_EVENT_BADGE_RED = 'bg-[color-mix(in_srgb,var(--color-error)_20%,transparent)] text-[var(--color-error)] border border-[color-mix(in_srgb,var(--color-error)_30%,transparent)]';
            const MOBILE_EVENT_BADGE_BLUE = 'bg-[color-mix(in_srgb,var(--color-primary)_20%,transparent)] text-[var(--color-primary)] border border-[color-mix(in_srgb,var(--color-primary)_30%,transparent)]';
            function mobileEventBadgeClass(eventType) {
                return (eventType === 'crash' || eventType === 'error') ? MOBILE_EVENT_BADGE_RED : MOBILE_EVENT_BADGE_BLUE;
            }

            // The live-refresh poll (every 5s, see refreshDashboardData) calls renderLogsTable()
            // on every tick regardless of whether anything actually changed — without this guard,
            // that meant a full tbody teardown/rebuild (and header rewrite) 12x/minute, which is
            // what caused the visible jerk: any hover, tooltip, or in-progress copy click in the
            // table got reset mid-interaction. Only the row *identity* (ids, in order) needs to
            // match for the signature to be equal — the row content itself is immutable once a
            // heartbeat check / mobile event is created, so same-ids-in-order guarantees identical
            // rendered output.
            let logsTableSignature = null;

            function renderLogsTable() {
                const tbody = document.getElementById('logsTableBody');
                const headRow = document.getElementById('logsTableHeadRow');
                if (!tbody) return;

                const m = monitors.find(x => x.id === activeId);
                const isMobile = !!m && m.type === 'mobile';
                updateLogsStatusFilterOptions(isMobile);

                if (isMobile) {
                    const statusEl = document.getElementById('logsStatusFilter');
                    const typeFilter = statusEl ? statusEl.value : 'ALL';
                    const dateEl = document.getElementById('logsDateFilter');
                    const dateFilter = dateEl ? dateEl.value : '';
                    const limitEl = document.getElementById('logsLimitFilter');
                    const limit = limitEl ? parseInt(limitEl.value, 10) : 20;

                    let events = mobileEventFeedCache || [];
                    events = events.filter(ev => matchesEventTypeFilter(ev, typeFilter));
                    if (dateFilter) events = events.filter(ev => ev.created_at && ev.created_at.slice(0, 10) === dateFilter);
                    events = events.slice(0, limit);

                    const signature = 'mobile:' + activeId + ':' + events.map(ev => ev.id).join(',');
                    if (signature === logsTableSignature) return;
                    logsTableSignature = signature;

                    if (headRow) {
                        headRow.innerHTML = `
                            <th class="py-2.5 px-4 rounded-l-xl">Timestamp</th>
                            <th class="py-2.5 px-4">Event</th>
                            <th class="py-2.5 px-4">Device</th>
                            <th class="py-2.5 px-4 rounded-r-xl">Details</th>
                        `;
                    }

                    tbody.innerHTML = '';
                    if (events.length > 0) {
                        events.forEach(ev => {
                            const tr = document.createElement('tr');
                            tr.className = 'hover:bg-[color-mix(in_srgb,var(--color-bg-surface)_30%,transparent)] transition-colors';
                            const badgeClass = mobileEventBadgeClass(ev.event_type);
                            // Crash/error names come straight from a thrown error's message (e.g. a
                            // React "Element type is invalid..." message can run hundreds of chars) —
                            // the badge just says "crash"/"error" for those, the actual message lives
                            // in the Details column (formatMobileEventDetails) where there's room for it.
                            const isCrashOrError = ev.event_type === 'crash' || ev.event_type === 'error';
                            const label = escapeHtml(isCrashOrError ? ev.event_type : (ev.event_name || ev.event_type));
                            tr.innerHTML = `
                                <td class="py-2.5 px-4 text-[var(--color-text-secondary)]">${ev.created_at ? new Date(ev.created_at).toLocaleString() : ''}</td>
                                <td class="py-2.5 px-4"><span class="px-2 py-0.5 rounded text-[10px] font-bold inline-block max-w-[240px] truncate align-bottom ${badgeClass}">${label}</span></td>
                                <td class="py-2.5 px-4 text-[var(--color-text-secondary)]">${formatMobileDevice(ev)}</td>
                                <td class="py-2.5 px-4 text-[var(--color-text-secondary)]">${formatMobileEventDetails(ev)}</td>
                            `;
                            tbody.appendChild(tr);
                        });
                    } else {
                        tbody.innerHTML = `<tr><td colspan="4" class="py-4 text-center text-[var(--color-text-muted)]">No mobile events recorded yet.</td></tr>`;
                    }
                    return;
                }

                const logs = getFilteredLogs();
                const signature = 'heartbeat:' + activeId + ':' + logs.map(log => log.id).join(',');
                if (signature === logsTableSignature) return;
                logsTableSignature = signature;

                if (headRow) {
                    headRow.innerHTML = `
                        <th class="py-2.5 px-4 rounded-l-xl">Timestamp</th>
                        <th class="py-2.5 px-4">Status</th>
                        <th class="py-2.5 px-4">Latency</th>
                        <th class="py-2.5 px-4">Details</th>
                        <th class="py-2.5 px-4 rounded-r-xl">Response</th>
                    `;
                }

                tbody.innerHTML = '';

                if (logs.length > 0) {
                    logs.forEach(log => {
                        const tr = document.createElement('tr');
                        tr.className = 'hover:bg-[color-mix(in_srgb,var(--color-bg-surface)_30%,transparent)] transition-colors';
                        const isError = log.status === 'ERROR';
                        const badgeClass = isError
                            ? 'bg-[color-mix(in_srgb,var(--color-error)_20%,transparent)] text-[var(--color-error)] border border-[color-mix(in_srgb,var(--color-error)_30%,transparent)]'
                            : 'bg-[color-mix(in_srgb,var(--color-primary)_20%,transparent)] text-[var(--color-primary)] border border-[color-mix(in_srgb,var(--color-primary)_30%,transparent)]';
                        tr.innerHTML = `
                        <td class="py-2.5 px-4 text-[var(--color-text-secondary)]">${log.timestamp ? new Date(log.timestamp).toLocaleString() : ''}</td>
                        <td class="py-2.5 px-4"><span class="px-2 py-0.5 rounded text-[10px] font-bold ${badgeClass}">${log.status}</span></td>
                        <td class="py-2.5 px-4 text-[var(--color-text-primary)]">${log.ms} ms</td>
                        <td class="py-2.5 px-4 text-[var(--color-text-secondary)] truncate max-w-xs">${log.msg}</td>
                        <td class="py-2.5 px-4 text-[var(--color-text-secondary)] max-w-xs">${formatResponseHeaders(log.response_headers, log.id)}</td>
                    `;
                        tbody.appendChild(tr);
                    });
                } else {
                    tbody.innerHTML = `<tr><td colspan="5" class="py-4 text-center text-[var(--color-text-muted)]">No heartbeat check events recorded.</td></tr>`;
                }
            }

            function togglePauseActive() {
                const m = monitors.find(x => x.id === activeId);
                if (!m) return;
                fetch(`/api/monitors/${m.id}/pause`, { method: 'POST' })
                    .then(r => r.json())
                    .then(res => {
                        m.is_paused = !m.is_paused;
                        openToast(m.is_paused ? `Paused ${m.name}` : `Resumed ${m.name}`);
                        renderActiveMonitor(activeId);
                    });
            }

            function editActive() {
                const m = monitors.find(x => x.id === activeId);
                if (!m) return;
                window.location.href = `/monitors/${m.id}/edit`;
            }

            function cloneActive() {
                const m = monitors.find(x => x.id === activeId);
                if (!m) return;
                const payload = {
                    name: `${m.name} (Copy)`,
                    type: m.type,
                    url: m.url,
                    hostname: m.hostname,
                    port: m.port,
                    interval_seconds: m.interval_seconds,
                    tags: m.tags
                };

                fetch('/api/monitors', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload)
                }).then(r => r.json()).then(res => {
                    if (res.success) {
                        openToast(`Cloned monitor: ${payload.name}`);
                        setTimeout(() => location.reload(), 800);
                    } else {
                        showToast(res.message || 'Failed to clone monitor', 'error');
                    }
                });
            }

            function deleteActive() {
                const m = monitors.find(x => x.id === activeId);
                if (!m) return;
                document.getElementById('deleteConfirmName').innerText = m.name;
                const modal = document.getElementById('deleteConfirmModal');
                const container = document.getElementById('deleteConfirmContainer');
                modal.classList.remove('hidden');
                setTimeout(() => container.classList.remove('scale-95', 'opacity-0'), 10);
            }

            function closeDeleteConfirmModal() {
                const modal = document.getElementById('deleteConfirmModal');
                const container = document.getElementById('deleteConfirmContainer');
                container.classList.add('scale-95', 'opacity-0');
                setTimeout(() => modal.classList.add('hidden'), 200);
            }

            function confirmDeleteActive() {
                fetch(`/api/monitors/${activeId}`, { method: 'DELETE' })
                    .then(r => r.json())
                    .then(res => {
                        closeDeleteConfirmModal();
                        openToast('Monitor deleted');
                        location.reload();
                    });
            }

            function resetActive() {
                const m = monitors.find(x => x.id === activeId);
                if (!m) return;
                document.getElementById('resetConfirmName').innerText = m.name;
                const modal = document.getElementById('resetConfirmModal');
                const container = document.getElementById('resetConfirmContainer');
                modal.classList.remove('hidden');
                setTimeout(() => container.classList.remove('scale-95', 'opacity-0'), 10);
            }

            function closeResetConfirmModal() {
                const modal = document.getElementById('resetConfirmModal');
                const container = document.getElementById('resetConfirmContainer');
                container.classList.add('scale-95', 'opacity-0');
                setTimeout(() => modal.classList.add('hidden'), 200);
            }

            function confirmResetActive() {
                fetch(`/api/monitors/${activeId}/reset`, { method: 'POST' })
                    .then(r => r.json())
                    .then(res => {
                        closeResetConfirmModal();
                        openToast('Monitor data reset');
                        location.reload();
                    });
            }

            function filterMonitors() {
                const status = document.getElementById('statusFilter').value;
                const type = document.getElementById('typeFilter').value;
                const tag = document.getElementById('tagFilter').value;

                monitors.forEach(m => {
                    const card = document.getElementById(`monitor-card-${m.id}`);
                    if (!card) return;

                    const matchStatus = status === 'ALL' || (status === 'PAUSED' && m.is_paused) || (m.status === status && !m.is_paused);
                    const matchType = type === 'ALL' || m.type === type;
                    const matchTag = tag === 'ALL' || (m.tags && m.tags.includes(tag));

                    card.style.display = (matchStatus && matchType && matchTag) ? 'block' : 'none';
                });
            }

            function openToast(msg) {
                showToast(msg, 'success');
            }

            function updateChartFilter() {
                const m = monitors.find(x => x.id === activeId);
                if (!m) return;
                updateResponseChart(m);
            }

            async function handleLogout(event) {
                if (event) event.preventDefault();
                try {
                    await fetch('/api/account/logout', { method: 'POST' });
                } catch (err) {
                    console.error('Logout request failed', err);
                }
                window.location.href = '/auth/login';
            }
        </script>
</body>

</html>